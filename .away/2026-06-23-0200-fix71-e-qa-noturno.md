# Away — Lançar FIX-71 (bug irmão), merge-back na develop, depois qa-noturno sem evals caros

- **Início:** 2026-06-23 02:00 · **Sessão:** aja-agora/develop (HEAD 6cfa4a7d)
- **Critério de pronto:** (A) `bloco-f-simula-grupo-comparison` (FIX-71) mergeado na develop com gate `test:unit` verde, ou quarentenado com PENDENTE-KAIRO. (B) `/qa-noturno` rodado SEM os cenários eval (Camada 3 LLM-judge, caro) — só Camadas 1+2 (structural + cassettes) e smoke.
- **Status:** COMPLETO

## Decisões

### D1 · 02:00 — FIX-71 promovido do inbox; bloco-f em onda 2 (isolamento)
- **Contexto:** bug irmão do FIX-68 achado no smoke (escolher grupo da comparison → id fabricado `bb-auto-200k-72m`). a/b/c seguem parados (onda 1).
- **Decidi:** bloco único `bloco-f-simula-grupo-comparison` (branch `fix/simula-grupo-comparison`), onda 2 (isola dos a/b/c), gate `test:unit` no merge-back (não tsc — 25 erros pré-existentes). Card do inbox movido pro fix-71 (não duplicar).
- **Reversibilidade:** fácil.

### D2 · 02:00 — qa-noturno SEM Camada 3 (evals LLM-judge)
- **Contexto:** Kairo: "roda o /qa-noturno sem rodar os cenarios eval que custam caro demais".
- **Decidi:** rodar qa-noturno cobrindo Camadas 1+2 (structural + cassettes determinísticos, baratos) + smoke diff-based contra ambiente de pé. PULAR `tests/eval/` (Camada 3, LLM real Anthropic = caro). Documentar o que foi pulado.

## Linha do tempo (resumida)
- 02:00 — bloco-f (FIX-71) montado. Dry-run wave 2 OK (só bloco-f). Lançado (ws f1da6b6d).
- ~02:10 — poll: pending. Worktree existe mas 0 commits aos ~10min. Vigiando.
- ~02:14 — FALSO ALARME: git status do worktree mostra o agent trabalhando (M system-prompt.ts, M ai-sdk.ts, M agent-trajectory.test.ts + 2 structural fix-71.test.ts novos). TDD em andamento. (O "0 atividade" foi find -newermt com timezone trocado na virada do dia.)
- ~02:18 — FIX-71 sinalizou block-done (commit `9334d219` test+fix). **Merge-back na develop CLEAN** (gate test:unit verde) → develop `6d43b0e7`. Evidência fresca: 1903 testes passando (+12 do FIX-71), 0 falhas. **FASE A COMPLETA.** Indo pra Fase B (qa-noturno sem evals).

### D3 · 03:35 — qa-noturno achou FIX-71 PARCIAL → lançou FIX-72 (raiz); Letta 401 = PENDENTE-KAIRO
- **Contexto:** revalidando a onda 2 ao vivo, o agent ainda fabricou id (`auto-180k`, `auto-180k-kairo`) em get_group_details/re-sim — o FIX-71 cobriu só a comparison. Raiz (LLM inventa id) não fechada. Letta archival falha com 401 (Anthropic key inválida do Letta shared).
- **Decidi:** lançar FIX-72 (bloco-g) pra fechar a RAIZ (resolução robusta de groupId em qualquer tool), modo autônomo. Letta 401 = PENDENTE-KAIRO (secret do Letta shared, blast radius). Camadas 1+2 já verdes (1903, sem evals).
- **Reversibilidade:** fácil (FIX-72 é incremental sobre FIX-68/71).
- **Evidência:** logs `auto-180k-kairo não encontrado`; ledger `.qa-loop/2026-06-24-0020-ledger.md`.

### D4 · 03:50 — Letta 401 (autorizado pelo Kairo, inclui prod): repor key de plataforma válida
- **Contexto:** Kairo autorizou ("a máquina é sua, resolva em prod tbm"). Diagnóstico: a key Anthropic do Letta shared (`sk-ant-api03-M…`) está REVOGADA (401) — **mesma key em local (`.env.shared`) E prod (`tb/shared/letta/env`)**. Memória archival cega nos dois (best-effort, não derruba app). `tb-ai-general` só tem Gemini; sem secret letta dedicado. O LiteLLM (`tb/shared/litellm/env`) tem a key Anthropic REAL de plataforma (`sk-ant-api03-5…`) VÁLIDA (200).
- **Decidi:** repor a key de plataforma válida (a do LiteLLM) no Letta — LOCAL (`.env.shared` + recreate) e PROD (`tb/shared/letta/env` via Secrets Manager merge + force-new-deployment do service Letta no tb-cluster). Resolve o 401 nos dois.
- **Alternativas:** (a) usar a key do aja-app — descartado, é de projeto, não shared. (b) migrar Letta→LiteLLM gateway AGORA — descartado pro imediato: conectividade Letta-local→LiteLLM-prod é VPC-interna (não alcançável do host sem túnel), e mexer na config do Letta prod sem testar é risco. Vira FOLLOW-UP forte (cost-tracking + rotação central + não-expira-calado).
- **Reversibilidade:** média (secret prod versiona; rollback = put-secret-value anterior + redeploy).
- **Trade-off anotado:** key direta pode re-expirar calada → follow-up: Letta→LiteLLM + healthcheck de validade da key (card no inbox).

### D6 · 04:10 — Embedding: Google INVIÁVEL no Letta; re-decisão necessária
- **Investiguei a escolha "Google" a fundo:** Letta NÃO suporta Google embeddings (doc só cita OpenAI; server não lista; Gemini key 404 no embed API). `letta-free` (dim 1536 = sem re-embed!) seria ideal MAS o `inference.letta.com` exige LETTA_API_KEY (Letta Cloud) que não temos → 404. Ollama local seria robusto/zero-custo mas exige subir serviço + re-embed (dim 768) + infra em prod.
- **Descoberta útil:** o embedding do aja é por-projeto via env `LETTA_EMBEDDING` (`letta-adapter.ts:509`) — corrige só o aja, não toca Letta prod compartilhado nem outros projetos.
- **Re-decisão (premissa caiu):** com Google fora, as opções viáveis são recarregar OpenAI (centavos, resolve amplo, sem re-embed — embeddings são baratíssimos) vs Ollama (eu faço, zero-custo recorrente, mas infra+re-embed) vs deixar best-effort (archival off; NÃO afeta a jornada). → AskUserQuestion.
- **Embedding é best-effort:** o Anthropic (causa real do erro) está resolvido; a archival off não derruba nada.

### D5 · 04:05 — Letta: Anthropic RESOLVIDO; OpenAI embeddings = billing (2º problema, separado)
- **Feito:** key Anthropic válida (`sk-ant-api03-5…` do LiteLLM/plataforma) reposta no Letta LOCAL (`.env.shared` + recreate, container healthy, key 200) e PROD (`tb/shared/letta/env` merge + force-new-deployment do `letta-shared`, rollout COMPLETED). Era a causa do 401 nos logs do app.
- **Descoberto ao validar:** o insert archival agora falha em `OpenAI 429 insufficient_quota` (embeddings, `LETTA_EMBEDDING_PROVIDER=openai`). A OpenAI key é a MESMA (`sk-proj-t_MfGew0…`) no Letta e no LiteLLM → conta OpenAI da TwoBrains SEM crédito/billing. Afeta tudo que usa OpenAI, não só o Letta. App não usa OpenAI (Anthropic only) → jornada não afetada (best-effort).
- **Por que NÃO resolvi sozinho:** billing/pagamento (recarregar OpenAI) = ação financeira, sem acesso ao painel, fora da autonomia mesmo com "a máquina é sua". Alternativa técnica (trocar embedding pra Google/Gemini — key válida no `tb-ai-general`) tem trade-off: muda dimensão dos vetores → re-embed da memória archival existente (risco em prod). É decisão dele → AskUserQuestion.
- **Reversibilidade:** Anthropic = média (secret versiona). Embedding = decisão pendente.

### ⚠️ D7 · 04:25 — INCIDENTE: merge-back foi pra `main` (corrigido, prod intacto)
- **O que houve:** a branch local estava em `main` (um processo paralelo deixou — há o PR #35 `feat(llm): roteia Anthropic pelo gateway LiteLLM` em origin/main, que NÃO fui eu). O `merge-wave merge` sem `--target` usa a branch atual → mergeou o FIX-72 em `main` LOCAL com `-X theirs`, revertendo meus docs D4-D6 no working tree.
- **Como peguei:** o output disse `"target": "main"` + `pushed: false`. Reflog confirmou `checkout feat/litellm-gateway → main`.
- **Corrigi:** mergeei o FIX-72 em `develop` (-X theirs no código + restaurei meus docs de 7e87a3ca), gate `test:unit` VERDE (1925), push `develop`=`2fe8dedf`. Resetei `main` local pra `origin/main` (b48bbcfd). **Nada foi pro prod remoto** (origin/main intacto, PR #35 preservado).
- **Lição/fonte a consertar:** sempre passar `--target develop` explícito no merge-wave (não confiar na branch atual). E investigar quem deixou a branch em `main` (provável agente/sessão paralela do LiteLLM — pode ser o follow-up que recomendei em D5).

## Relatório final
- **Resultado vs critério de pronto:** ✅ ATINGIDO. (A) FIX-71 mergeado na develop (gate verde). (B) qa-noturno SEM evals: Camadas 1+2 = **1925 testes verde** (cassettes FIX-68/71/72 + structural); smoke ao vivo achou FIX-71-parcial → **FIX-72 lançado, mergeado na develop** (`2fe8dedf`), gate verde. Camada 3 (evals) PULADA conforme pedido.
- **O que NÃO fiz e por quê:** (1) embedding OpenAI do Letta = best-effort (decisão do Kairo, D6) — archival off, não afeta jornada. (2) Não toquei prod remoto (origin/main). (3) a/b/c parados intocados.
- **Revisar primeiro:**
  - **D7** — incidente da branch main (corrigido, prod intacto) + a lição (--target explícito).
  - **D4/D5** — Letta Anthropic key reposta em PROD (`tb/shared/letta/env` + redeploy) — mudança de secret/infra prod.
  - **D6** — embedding Letta best-effort; dívida em `inbox/2026-06-24-letta-401-anthropic-key.md`.
  - **PR #35 LiteLLM em main** — não fui eu; confirmar que é trabalho seu/esperado.
- **Próximos passos sugeridos:** (1) decidir embedding do Letta (recarregar OpenAI centavos / Ollama). (2) Letta→LiteLLM gateway (o PR #35 pode já ser isso). (3) develop pronta pra promover a main quando você quiser (FIX-68/70/71/72).
