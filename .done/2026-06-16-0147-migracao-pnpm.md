# Migração pnpm + arquitetura dev local — aja-agora

**Branch:** `chore/migracao-pnpm-arquitetura-dev` (pushada; sem PR/merge, por desenho)
**Data:** 2026-06-16
**Tipo:** migração mecânica de infra (package manager + dev local), zero mudança de produto

## O que mudou

A plataforma deixa de usar npm e passa a `pnpm` como gestor único, alinhando ao
padrão global TwoBrains que evita "fritar o Mac" (npm instala `node_modules`
cheio por imagem/worktree → custo linear de tempo e disco com N agentes paralelos).
Com pnpm + store content-addressable compartilhado, cada pacote existe 1× em disco
e o resto é hardlink → subir workspace novo é quase instantâneo, ~zero disco extra.

### A — Package manager → pnpm
- `pnpm import` do `package-lock.json` → `pnpm-lock.yaml`; `package-lock.json` removido.
- `package.json`: `packageManager: pnpm@11.7.0`, `engines.pnpm >= 11`, script `typecheck`
  novo, e scripts internos `npm run` → `pnpm`.
- `pnpm-workspace.yaml`: `allowBuilds` liberando os build scripts nativos
  (esbuild, sharp, msgpackr-extract).
- `.npmrc`: `node-linker=hoisted` (ver "Decisões" — necessário pro build funcionar).
- `tests/regression/migrate-guard.test.ts`: `npm run` → `pnpm run` no execSync.
- `README.md`: getting-started reescrito pra pnpm + fluxo container.

### B — Dockerfile.dev sem install embutido
- Removido `COPY package*.json` + `RUN npm ci`. Agora: corepack pnpm, `PNPM_STORE_DIR`,
  e o install roda no **boot** (`CMD pnpm install --frozen-lockfile && pnpm dev`).

### C — docker-compose (dev)
- App: `node_modules` em **named volume** `app_node_modules` (não mais anônimo),
  store pnpm externo compartilhado `pnpm_store → tb-pnpm-store-shared` em `/pnpm/store`,
  env `PNPM_STORE_DIR`.

### D — Dockerfile (build prod/standalone)
- `# syntax=docker/dockerfile:1.7`, corepack pnpm no stage `base`.
- Install no layer da imagem (antes do `COPY . .` → camada de deps cacheada por lockfile),
  `pnpm db:migrate:bundle` + `pnpm build`. Entrypoint (`docker-entrypoint.sh`) usa
  `pnpm run`/`pnpm exec`.

### E — CLAUDE.md
- Seção "Package manager — pnpm ÚNICO" + refs `npx`/`npm` → `pnpm dlx`/`pnpm exec`.
- `.husky/pre-commit` passou a rodar via `pnpm` (não `npm`).

## Validação rodada (evidência)

| Check | Resultado |
|---|---|
| `pnpm install --frozen-lockfile` | ✅ exit 0, lockfile consistente |
| Build scripts nativos (esbuild/sharp/msgpackr) | ✅ rodam via `allowBuilds` |
| **Build imagem DEV** (`Dockerfile.dev`) | ✅ OK |
| **Build imagem PROD** (`Dockerfile`) | ✅ deps + `esbuild`/`db:migrate:bundle` + **Next compile (56s)** OK; barra só no type-check pré-existente (ver gaps) |
| `docker compose --profile containerized config` | ✅ exit 0 — volumes nomeados + store externo + `PNPM_STORE_DIR` corretos |
| **`pnpm test:unit`** (Camadas 1+2, suite inteira sobre node_modules do pnpm) | ✅ **1702 passed \| 4 skipped** |
| pre-commit hook (3 commits) | ✅ verde / pulado em commits docs-only |

> O `test:unit` exigiu subir o Postgres do workspace + migrar (4 testes de
> integração tocam DB real). Feito via `docker compose up -d db` + `pnpm db:migrate`
> (local, descartável) e `.env.local` host-only; **derrubado no fim** (`down -v`,
> `.env.local` removido, imagens de validação removidas).

## Decisões tomadas

- **`node-linker=hoisted` (em vez de `enableGlobalVirtualStore`/layout isolado):** dois
  bloqueios reais do layout padrão do pnpm neste projeto — (1) o postinstall do
  `esbuild` extrai um binário nativo de ~10 MB que **some** quando o store fica num
  BuildKit cache mount; (2) Next 16 (Turbopack) **não resolve `next/package.json`**
  a partir de `src/app` no node_modules symlinkado; e `output: standalone` faz
  tracing de node_modules. Flat resolve os três e é o ambiente sob o qual o projeto
  foi construído. O anti-fritar é preservado: o node_modules flat **hardlinka do
  store compartilhado** → 1 cópia física global. Por isso `enableGlobalVirtualStore`
  (exclusivo do layout isolado) **não** é usado aqui — desvio documentado no
  `pnpm-workspace.yaml` e no `.npmrc`.
- **Build prod sem BuildKit cache mount no store:** o binário auto-extraído do esbuild
  não sobrevive ao mount destacando ao fim do RUN. Store no próprio layer → build
  reprodutível. Rebuild rápido continua coberto pelo cache de layer do Docker
  (install antes do `COPY . .`). O par `pnpm fetch` + `install --offline` do template
  tb-local-dev **também** falhou (não materializa optional deps nativos por plataforma
  a partir de lockfile importado de npm) — descartado pelo mesmo motivo.
- **Comando dev do projeto:** `pnpm dev` (Next dev/Turbopack), porta 3000, hostname 0.0.0.0.
- **Single-package** (não monorepo): `pnpm-workspace.yaml` existe só pra fixar `allowBuilds`.
- **Docs históricos** (`.away/`, `docs/specs/*qa-report*`, `docs/correcoes/done/`,
  `docs/test-plan*`) **não** foram reescritos — são registro point-in-time; trocar
  comandos npm→pnpm neles é revisionismo sem valor. Só docs operacionais ativos
  (README, CLAUDE.md, local-dev) foram atualizados.

## Gaps honestos

- **`pnpm build` / `pnpm typecheck` NÃO passam end-to-end na branch — por débito de
  tipo PRÉ-EXISTENTE, não pela migração.** O `next build` type-checa e quebra em
  `src/lib/eval/scorer.ts:67`: o tipo `LeadStage` (em `src/lib/eval/signals.ts` /
  `src/lib/memory/types.ts`) ficou dessincronizado do enum do schema após o FIX-43
  (`na_administradora` existe em `src/db/schema.ts` mas não no tipo). `git diff HEAD`
  nesses arquivos é **vazio** — a migração não os tocou; falharia idêntico sob npm
  (o build GHA atual da branch está nessa mesma condição). O pipeline pnpm está
  **provado correto** (deps + esbuild + Next compile passam). Fix do tipo é trabalho
  de produto à parte (TDD), fora do escopo desta migração — **não** foi embutido aqui
  pra não mascarar o débito num commit `build:`.
- **`pnpm lint` (biome) tem 120 erros pré-existentes** em arquivos não tocados
  (ex.: `drizzle/meta/_journal.json` gerado). Meus arquivos passam limpos no biome.
- **Stack dev completa (`up --profile containerized`) não foi subida** end-to-end —
  exige bootstrap de secrets (Letta shared, Bevi, better-auth). Validado em vez disso:
  build das imagens dev+prod, `compose config`, e suite de testes verde sobre pnpm.

## Próximo passo (humano)
Revisar o diff + decidir merge. A branch sobe build prod até o compile; antes de
mergear/deployar, o time deve corrigir o tipo `LeadStage` pré-existente (destrava
o `next build`). Reminder criado.
