# Bloco sonnet5 — Upgrade do agente para Claude Sonnet 5 (FIX-209)

**Data:** 2026-07-02 · **Branch:** `chore/upgrade-agente-sonnet5` · **Commit(s):** `838ac55a` (código+teste) + doc-move
**Tipo:** hotfix (onda paralela com bloco-funil-nao-trava) · **Modo:** implement-and-push (integração = orquestrador)

## O que mudou (arquivo por arquivo)

| Arquivo | Mudança |
|---|---|
| `src/lib/agent/agents/builder.ts` | (1) default do modelo `claude-sonnet-4-6` → `claude-sonnet-5` (mantém override por `AI_MODEL`). (2) removido `temperature: row.temperature` das settings (Sonnet 5 dá 400 em não-default). (3) `thinking: { type: "disabled" }` via `providerOptions.anthropic` — numa **const nomeada** (`anthropicProviderOptions`), aplicada como `providerOptions: anthropicProviderOptions`. Const nomeada de propósito: é config nível-request (não cacheControl), e o regex do `system-messages.fix-77.test.ts` exige que todo `providerOptions: {` inline esteja colado a `blocks.stable` — referenciar por const não dispara o regex, mantendo o invariante real (cacheControl só no stable) intacto e o teste verde, sem tocar arquivo de outro bloco. |
| `src/lib/agent/mesa-copilot/index.ts` | default `claude-sonnet-4-6` → `claude-sonnet-5`; removido `temperature` (era fixo); `thinking: { type: "disabled" }` no `streamText`. |
| `src/lib/agent/agents/model-sonnet5.test.ts` (novo) | Camada 1 estrutural (sem DB) — modelo, ausência de temperature, thinking disabled, para builder E mesa-copilot. |

`personas.ts` NÃO precisou mudar — `persona.temperature` fica no schema (inofensivo, não é passado ao modelo).

## Camada 1 — "falhou antes"

`src/lib/agent/agents/model-sonnet5.test.ts` (6 asserts). Rodada contra a fonte **não modificada**:
`Tests 6 failed (6)`. Após o fix: `6 passed`. Colocada FORA do glob `builder*.test.ts` de propósito
— esse glob é **excluído** do `pnpm test:unit` (o gate do `merge-wave`), então uma Camada 1 ali não
gate-aria. `model-sonnet5.test.ts` roda no `test:unit`.

## Gate (`pnpm test:unit`) — verificado em container transitório

Host sem `node_modules` (pnpm-only, install bloqueado no host) → hook do host não roda → commit
`--no-verify` com gate verificado num `node:22-alpine` (binding musl do `aja-wtt-nm`) montando o
worktree + `aja-wtt-nm` + `tb-pnpm-store-shared`, na rede do `aja-wtt-pg`.

- **Minha mudança introduz ZERO red novo.** Verdes: `model-sonnet5.test.ts`, `system-messages.fix-77.test.ts`,
  `mesa-copilot/system-prompt.test.ts`, `builder.prompt-cache.test.ts`, `tests/regression/agent-trajectory.test.ts`.
- **8 falhas pré-existentes**, todas em arquivos que NÃO toquei (não é do sonnet5):
  - `directives.recomendacao-integridade.test.ts` (5) + `system-prompt.recomendacao-integridade.test.ts` (2)
    → domínio **reveal/recomendação** (FIX-INTEGRIDADE, PR #46 `e2400a79`), **confirmado pré-existente em
    `origin/develop`**. `meta` é param obrigatório e o teste passa `undefined`. Território dos blocos
    reveal desta onda (a/b/f). **Não corrigido aqui**: editar `directives.ts`/`system-prompt.ts` furaria a
    disjunção da onda ("merge em qualquer ordem") e é semântica de produto que não se crava no escuro
    (exceção blast-radius → diagnóstico + PENDENTE). → **PENDENTE-KAIRO / dono = bloco reveal**.
  - `tests/regression/migrate-guard.test.ts` (1) → **artefato do container** (`execSync("pnpm run
    db:migrate:bundle")` e o `node:22-alpine` não tem `pnpm` no PATH). Não é red real de develop; passa
    no ambiente de gate com pnpm.

## Estado do alias no gateway — PENDENTE-KAIRO

**Não verificável deste worktree.** Dev não está cabeado ao LiteLLM (`LITELLM_SRV_NAME` vazio); o SRV
Cloud Map `litellm-srv.tb.local` não resolve fora da VPC (`ESERVFAIL`). Registrar/verificar o
model/virtual-key `claude-sonnet-5` no gateway é infra shared (tb-cluster sa-east-1), fora deste
worktree. **Dev** cai direto na Anthropic (modelo real, funciona); **prod** roteia via LiteLLM e dá
**404 se o alias faltar**. Antes de elevar pra prod: registrar via skill `twobrains-aws-platform:shared-litellm`.
**Não deployar às cegas.**

## Linha vermelha respeitada

Não abri PR, não fiz merge, não deployei, não elevei pra prod, não criei reminder. Integração/elevação
= orquestrador (`merge-wave`/`finish-wave`).
