# F3 admin-régua — AJA-03, AJA-04, AJA-09 (UI), AJA-10 (UI), AJA-11, badges

**Status:** concluído · 2026-09-18 · branch `kairogyn/f3-admin-regua`

## O que foi entregue

- **Badges**: `success`/`warning` por token em `badge.tsx`; `default` (coral) deixa de representar estado no admin.
- **Dicionários únicos**: `motivo-fora-da-regua.ts` (exclusão + saída, ícones, "Fora da régua · …") e `rotulo-do-bem.ts` (Carro/Moto/Imóvel).
- **Lista de Conversas**: coluna **Remarketing** (um `leftJoin`, sem N+1) com passo/próximo toque ou motivo nomeado; Canal "WhatsApp"; "Bem"; pills Teste/Equipe; switch "Mostrar testes e equipe"; filtro `origem=desconhecida`. Qualidade saiu da lista.
- **Ficha**: bloco "Régua de remarketing" (linha do tempo + Segurar/Soltar) e confirmação no "Marcar como teste".
- **Tela da Régua**: resumo agregado no topo (`resumoDaRegua`), lista em seguida, funil/atribuição no fim; Ligada/Desligada; colunas renomeadas; "Qual toque converte" colapsável.
- **Percurso**: coluna Régua, "N fora, M na régua", telefone mascarado, "Enviar para a mesa" em lote (rota nova; só WhatsApp).
- **Sidebar** reagrupado; **cadastro da régua** com "Editado"/"Padrão de fábrica" e indicador ligada/desligada.

## Prova

- `pnpm vitest run --no-file-parallelism src/lib/admin src/app/api/admin/conversations src/app/api/admin/percurso src/components/admin/app-sidebar*` → 38 arquivos / 397 testes verdes.
- `pnpm typecheck`, `pnpm lint`, `pnpm test:unit` (523/4183), `pnpm test:caminho-do-dinheiro` (97/506) verdes.
- `grep variant="default"` e cores Tailwind cruas nos diretórios do escopo → vazios.

## Commits

- `03015313`, `afdb8daf` (merge do fix do gate do F2), `4571194a`.

## Pendências (válvula)

Unificação com o canônico do F6 (`motivo-de-exclusao.ts`), reconciliação das rotas `/api/admin/remarketing/**` tocadas, item "Exportação e dados" na sidebar quando o F8 existir, e o `grep "Whatsapp\b"` remanescente (identificadores em arquivos fora do escopo).