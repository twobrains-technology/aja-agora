# Goal concluído — F5 campanhas (AJA-06, AJA-07, AJA-08, AJA-17-campanhas)

Resumo em 10 linhas:

1. **AJA-07** — `src/lib/meta-ads/rotulo-legivel.ts`: `decodificarChaveDeCampanha` (`+`→espaço ANTES do `decodeURIComponent`,
   tolerante a `%` inválido, colapsa espaços). Só apresentação: `linha.chave`, `title` e a busca seguem com o cru. A chave real do
   print (`BOFU+-+AJA+%7C+…+C%C3%B3pia`) decodifica para `BOFU - AJA | CPM FOCO CONVERSÃO + BASE GERAL — Cópia`.
2. Na tabela, o texto principal é o decodificado; badge `outline` + `TriangleAlert` "Nome pendente do gerenciador"; id numérico puro
   → "Campanha nova · nome pendente".
3. **AJA-08** — `custoPor` deixou de devolver `null`/`"sem base"` e devolve `{ tipo: "valor" } | { tipo: "motivo", motivo }` com
   `sem_vinculo` (sem visita/conversa do CRM), `sem_gasto` (spend 0/nulo) e `sem_qualificado` (gasto e vínculo, zero qualificado).
4. `formato.ts` imprime "Sem qualificado no período" (`Minus`) · "Sem gasto informado" (`WalletCards`) · "Sem vínculo com o CRM"
   (`Unlink`), com tooltip de "o que fazer". A descrição da tabela muda para "Ordenado por investimento — ainda não há lead
   qualificado para calcular custo." quando nenhuma campanha tem custo calculável.
5. Cartão Meta × CRM reescrito: **Leads no CRM em destaque** (número grande), "Leads que a Meta atribuiu" ao lado e a diferença
   nomeada ("a Meta contou +N — cliques que não viraram conversa").
6. **AJA-17-campanhas** — linha fixa "Sem origem conhecida" no fim da tabela: conversas com `visit_id IS NULL` e `is_simulated=false`
   no período; `totalizarCampanhas` soma as conversas dessas linhas, então o total do CRM em Campanhas bate com a tela de Conversas.
7. **AJA-06** — `lerAnuncios()` pede `creative{id,name,thumbnail_url,effective_object_story_id}`; `MetaAdsError` carrega o código e
   `ehErroDeCampoNaoPermitido` reconhece #100/#200; o ciclo relê os anúncios sem criativo e loga UMA vez por ciclo.
8. Espelho `meta_entities` ganhou `creative_id`, `creative_name`, `thumbnail_url` (migration `0057_dashing_havok.sql`), com upsert.
9. A linha da campanha expande a sub-tabela **Criativos** (nome ou id cru + `ImageOff` "Nome pendente" · Visitas · Iniciaram conversa ·
   Identificados · miniatura 40×40), casando `visits.utm_content` ↔ `meta_entities.entity_id` de nível "ad"; sem dado,
   "Criativo não informado pelo anúncio."
10. **Colunas** prioritárias fixas (Campanha · Investimento · Custo/qualificado · Leads no CRM · Qualificados) e o resto em
    "Mais colunas" (`Collapsible`); subtítulo de 3 linhas + janela de atribuição virou uma frase + `Tooltip`.

## Commits (branch `kairogyn/f5-campanhas`, sem push)

- `e1d1ee6b` — feat(admin): campanhas com rotulo decodificado, motivo do custo e linha sem origem
- `55a5f7c6` — Merge branch 'kairogyn/implementacoes-funil-e-template' into kairogyn/f5-campanhas (rename do cenário, pedido do líder)
- `a49bf913` — feat(admin): criativos por anuncio na tela de campanhas (AJA-06)

## Comandos e saída resumida

- `pnpm vitest run --reporter=verbose --no-file-parallelism src/lib/meta-ads src/lib/admin/campanhas-queries.test.ts \
   src/lib/admin/campanhas-queries.integration.test.ts src/components/admin/campanhas` → **7 arquivos, 60 testes, todos verdes**.
- `grep -rn '"sem base"' src/` → **vazio**.
- `pnpm db:generate` → "No schema changes, nothing to migrate"; migration `0057_dashing_havok.sql` aplicada com `pnpm db:migrate`.
- `pnpm typecheck` → limpo. `pnpm lint` → 1442 arquivos, 0 erros/avisos.
- Gate de pré-commit (`pnpm test:pre-commit`, no commit `a49bf913`) → `test:unit` **524 arquivos / 4176 testes verdes**;
  `test:caminho-do-dinheiro` **96 arquivos / 507 testes verdes**.
- `git status --short` → limpo.

## Pendências da válvula (`.orientacao/pendencia-integracao.md`)

1. `performance-queries.ts` (F1): exportar o fragmento `atribuida`. O complemento (`visit_id IS NULL`) foi escrito inline em
   `campanhas-queries.ts` (`conversasSemOrigemConhecida`), com comentário; trocar por import quando a F1 exportar.
2. `src/db/schema.ts`: append de 3 colunas em `meta_entities` (frente F5) — migration 0057; nenhuma coluna existente alterada.
3. `cenario-nome-oferecido-chega-ao-banco` falhava o gate por timeout de 20s sob a carga da máquina; resolvido pelo líder com o
   rename para `.integration.test.ts` no merge `55a5f7c6`. Nenhuma correção local foi necessária.
