- `src/db/schema.ts` · adicionar `messages.metadata` (jsonb, nullable) · o goal dizia "messages.metadata (JSON já existente)", mas o JSON existente é de `conversations`; `messages` não tinha coluna de metadata (só `mediaKey/mediaType/mediaMimeType/mediaFilename`) · adicionei a coluna (append, sem alterar nenhuma existente) e gerei `drizzle/0057_hesitant_unus.sql` via `pnpm db:generate`, aplicada com `pnpm db:migrate`.
- `src/lib/agent/langgraph/nodes/persist.ts` (e `run-turn.ts`/`state.ts`/`adapter.ts`) · o caminho "canônico" para a linha única seria o `persist` do turno aceitar um `metadata` de mídia; esses arquivos são de outros agentes · contornei dentro do meu escopo: o turno grava a fala como sempre e `midia-do-cliente.ts` ANEXA o áudio nela (`db.update` em `messages`, casando pela última fala do cliente na conversa) — uma linha só, sem editar arquivo fora do escopo.
- `src/lib/agent/langgraph` (TurnTrace) · span `transcricao` no Langfuse (duração/bytes/modelo) · a transcrição roda em `midia-do-cliente.ts`, que está FORA do turno — não há `TurnTrace` aberto nesse ponto · não instrumentei; fica para quem conhece o ponto de span. O que existe hoje: log estruturado `[midia-do-cliente] áudio transcrito … (modelo, duracaoMs, bytes)` e `metadata.transcricao` no banco.
- Gateway LiteLLM · rota/modelo de ASR · `curl -m 5 http://127.0.0.1:4100/health` → 401 (vivo) e `POST /audio/transcriptions` → 401 (a ROTA EXISTE; não é 404). Com a chave do `.env.local` (`sk-local-dev`) o gateway recusa o token (`token_not_found_in_db`), então não deu para confirmar o `whisper-1` de ponta a ponta · segui com fake nos testes. O líder precisa garantir token e modelo no gateway.
- `src/lib/agent/system-prompt.ts` · veio alterado no merge `bbd816e1` (branch `kairogyn/implementacoes-funil-e-template`, frente f9-fala) · não é minha frente, mas lembra a regra do CLAUDE.md: mudança de `SYSTEM_PROMPT` só vale em produção depois de `pnpm sync-prompts` contra a instância certa.
