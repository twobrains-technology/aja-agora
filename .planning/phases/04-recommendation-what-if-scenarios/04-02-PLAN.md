---
phase: 04-recommendation-what-if-scenarios
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/agent/system-prompt.ts
autonomous: true
requirements:
  - CHAT-10

must_haves:
  truths:
    - "User says 'e se eu mudar pra R$ 1000/mes' and agent recalculates using existing simulate_quota tool"
    - "Agent presents updated simulation via present_simulation_result within 3 seconds"
    - "Agent briefly compares new scenario with previous one"
    - "Agent does NOT re-run search_groups for simple parameter changes"
    - "Agent uses present_recommendation after recommend_groups to show the final recommendation card"
  artifacts:
    - path: "src/lib/agent/system-prompt.ts"
      provides: "What-if scenario detection instructions and recommendation presentation instructions"
      contains: "Cenarios What-If"
  key_links:
    - from: "src/lib/agent/system-prompt.ts"
      to: "Agent behavior"
      via: "System prompt instructions guide tool selection"
      pattern: "simulate_quota.*present_simulation_result"
---

<objective>
Add what-if scenario detection and recommendation presentation instructions to the agent's system prompt, enabling real-time parameter exploration and proper recommendation delivery.

Purpose: CHAT-10 — users should be able to say things like "e se eu mudar pra R$ 1000/mes" and get instant recalculation using existing tools, without the agent re-searching from scratch. Also instructs the agent to use `present_recommendation` when delivering final recommendations.
Output: Updated `SYSTEM_PROMPT` in `src/lib/agent/system-prompt.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-recommendation-what-if-scenarios/04-CONTEXT.md
@.planning/phases/04-recommendation-what-if-scenarios/04-RESEARCH.md

<interfaces>
<!-- Current system prompt structure. -->

From src/lib/agent/system-prompt.ts:
```typescript
export const SYSTEM_PROMPT = `Voce e o assistente do Aja Agora...

## Sua Personalidade
...

## Como Conduzir a Conversa
1. Entenda o sonho
2. Busque opcoes
3. Apresente resultados
4. Aprofunde
5. Simule
6. Recomende: Quando tiver informacoes suficientes, use recommend_groups...

## Regras sobre Dados Financeiros — OBRIGATORIO
...

## Disclaimers BACEN — OBRIGATORIO
...

## O que Voce NAO Faz
...

## Formato de Resposta
...
`;
```

Available tools the agent can call (from route.ts allowedTools):
- mcp__consorcio__search_groups
- mcp__consorcio__simulate_quota
- mcp__consorcio__get_rates
- mcp__consorcio__get_group_details
- mcp__consorcio__recommend_groups
- mcp__consorcio__present_group_card
- mcp__consorcio__present_comparison_table
- mcp__consorcio__present_simulation_result
- mcp__consorcio__present_recommendation (added by Plan 01)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add what-if scenario detection and recommendation presentation instructions to system prompt</name>
  <files>src/lib/agent/system-prompt.ts</files>
  <action>
Append two new sections to the `SYSTEM_PROMPT` template literal, BEFORE the closing backtick. Add them after the existing "## Formato de Resposta" section.

**Section 1: "## Cenarios What-If"**

```
## Cenarios What-If
Quando o usuario quiser explorar cenarios alternativos — frases como "e se eu mudar pra R$ 1000/mes", "e se fosse 48 meses", "quero pagar menos", "e com outro valor", "muda o prazo", "e se fosse um carro mais barato":

1. Identifique qual parametro mudou (orcamento mensal, prazo, valor do credito, categoria)
2. Para mudancas simples (valor mensal ou prazo dentro do MESMO grupo): use simulate_quota com os novos parametros e depois present_simulation_result para mostrar o novo calculo
3. Para mudancas que alteram a busca (categoria diferente, faixa de credito muito diferente): use search_groups + recommend_groups + ferramentas de apresentacao
4. Compare brevemente com o cenario anterior, mencionando a diferenca principal ("Com R$ 1.000/mes a parcela sobe X%, mas o credito aumenta Y%")
5. IMPORTANTE: Para cenarios simples, va DIRETO ao simulate_quota — NAO refaca search_groups. Velocidade e essencial: o usuario espera resposta em menos de 3 segundos
6. Se o usuario pedir multiplas variacoes seguidas, mantenha o contexto do grupo original e varie apenas o parametro pedido
```

**Section 2: "## Recomendacao Final"**

```
## Recomendacao Final
Quando voce tiver informacoes suficientes sobre o que o usuario quer (categoria, orcamento mensal, prazo desejado):

1. Use recommend_groups para obter o ranking de compatibilidade
2. Use present_recommendation para mostrar o TOP 1 resultado como card visual, incluindo TODOS os campos: id, administradora, category, creditValue, monthlyPayment, adminFeePercent, termMonths, contemplationRate, score e scoreBreakdown completo (monthlyFit, contemplation, adminFee, termMatch)
3. Explique brevemente (1-2 frases) por que este grupo e o mais compativel com o perfil do usuario
4. Se o usuario ja viu uma simulacao detalhada do mesmo grupo, NAO repita os numeros — foque na recomendacao e no botao de acao
5. Se o usuario disser "tenho interesse" ou similar apos ver a recomendacao, reconheca o interesse e diga que em breve ele podera prosseguir com a adesao (funcionalidade de conversao sera adicionada em breve)
```

Do NOT modify any existing sections of the system prompt. Only append the two new sections.
  </action>
  <verify>
    <automated>cd /Users/kairo/code/personal/twobrains/aja-agora && npx tsc --noEmit 2>&1 | head -10 && grep -c "Cenarios What-If" src/lib/agent/system-prompt.ts && grep -c "Recomendacao Final" src/lib/agent/system-prompt.ts</automated>
  </verify>
  <done>System prompt contains "Cenarios What-If" section with instructions to go directly to simulate_quota for simple parameter changes, and "Recomendacao Final" section instructing the agent to use present_recommendation with full score data. TypeScript compiles clean. Both sections grep-confirmed present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| System prompt -> Agent behavior | Prompt engineering guides tool selection; agent still validates via Zod schemas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Spoofing | Agent bypasses what-if instructions | accept | Prompt instructions are best-effort guidance. Agent may occasionally re-search instead of going direct to simulate_quota, which only affects latency (3s budget), not correctness. Existing Zod validation on all tools prevents invalid data. |
| T-04-05 | Repudiation | Agent fabricates score data in present_recommendation | mitigate | System prompt section "Regras sobre Dados Financeiros" already prohibits fabricating numbers. New "Recomendacao Final" section instructs agent to pass exact data from recommend_groups output. Zod schema on present_recommendation validates score range 0-1. |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes
2. `grep "Cenarios What-If" src/lib/agent/system-prompt.ts` returns 1 match
3. `grep "Recomendacao Final" src/lib/agent/system-prompt.ts` returns 1 match
4. Run dev server, start a conversation, get a recommendation, then say "e se eu mudar pra R$ 1000/mes" — agent should call simulate_quota + present_simulation_result without re-running search_groups
5. Response time for what-if scenario < 3 seconds (no unnecessary tool chain)
</verification>

<success_criteria>
- System prompt includes what-if scenario detection instructions
- Agent goes directly to simulate_quota for simple parameter changes (no search_groups re-run)
- System prompt includes recommendation presentation instructions referencing present_recommendation
- Agent delivers present_recommendation with full score and scoreBreakdown data
- No existing system prompt sections were modified
</success_criteria>

<output>
After completion, create `.planning/phases/04-recommendation-what-if-scenarios/04-02-SUMMARY.md`
</output>
