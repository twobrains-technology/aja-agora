---
phase: 06-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - src/components/landing/hero-section.tsx
  - src/components/landing/how-it-works.tsx
  - src/components/landing/benefits-section.tsx
  - src/components/landing/social-proof.tsx
  - src/components/landing/cta-section.tsx
  - src/components/landing/footer.tsx
  - src/components/landing/scroll-fade.tsx
  - src/components/landing/faq-section.tsx
autonomous: true
requirements: [LAND-01, LAND-02, LAND-03, LAND-04]

must_haves:
  truths:
    - "Landing page renders at / with hero section visible above the fold"
    - "CTA button 'Comecar agora' navigates to /chat"
    - "Como Funciona section shows 3-step visual flow"
    - "Benefits section lists product advantages"
    - "Social proof section shows testimonials and trust indicators"
    - "FAQ section answers common consorcio questions"
    - "Footer shows company info and links"
    - "All sections are responsive on 320px viewport"
    - "Scroll animations fade in below-fold sections"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Landing page composing all sections"
      min_lines: 20
    - path: "src/components/landing/hero-section.tsx"
      provides: "Hero with headline, sub-headline, CTA"
    - path: "src/components/landing/how-it-works.tsx"
      provides: "3-step como funciona visual"
    - path: "src/components/landing/benefits-section.tsx"
      provides: "Benefits grid"
    - path: "src/components/landing/social-proof.tsx"
      provides: "Testimonials and trust indicators"
    - path: "src/components/landing/cta-section.tsx"
      provides: "Final CTA before footer"
    - path: "src/components/landing/footer.tsx"
      provides: "Site footer"
    - path: "src/components/landing/scroll-fade.tsx"
      provides: "Reusable scroll-triggered animation wrapper"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/landing/*"
      via: "direct imports"
      pattern: "import.*@/components/landing"
    - from: "hero-section.tsx"
      to: "/chat"
      via: "next/link CTA button"
      pattern: 'href="/chat"'
---

<objective>
Build all landing page sections using shadcn/studio Pro blocks, compose them into the root page, and add scroll-triggered animations.

Purpose: Replace the current /chat redirect with a high-conversion landing page that communicates the product value and routes users to the chat experience.
Output: Complete landing page at / with hero, como funciona, benefits, social proof, FAQ, CTA, and footer sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-landing-page/06-RESEARCH.md
@.planning/phases/06-landing-page/06-CONTEXT.md
@.planning/phases/03-chat-ui-artifact-rendering/03-UI-SPEC.md
@src/app/globals.css
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scroll-fade animation wrapper and install/build landing page sections</name>
  <files>
    src/components/landing/scroll-fade.tsx,
    src/components/landing/hero-section.tsx,
    src/components/landing/how-it-works.tsx,
    src/components/landing/benefits-section.tsx,
    src/components/landing/social-proof.tsx,
    src/components/landing/faq-section.tsx,
    src/components/landing/cta-section.tsx,
    src/components/landing/footer.tsx
  </files>
  <action>
  **MANDATORY FIRST:** Use shadcn/studio Pro MCP tools to search and install relevant blocks before writing any code:
  1. Call `mcp__shadcn-studio-mcp__get-blocks-metadata` to list available blocks
  2. For each block type needed (hero-section, features-section, bento-grid, social-proof, testimonials, faq, cta-section, footer), call `mcp__shadcn-studio-mcp__get-block-meta-content` to inspect variations
  3. Select the most appropriate variation for each section
  4. Use `/cui` or `/iui` workflow to install/adapt blocks

  If MCP tools are unavailable or blocks don't fit, build sections following the block structure patterns but ALWAYS prefer Pro blocks first.

  **ScrollFade wrapper** (`scroll-fade.tsx`):
  - "use client" component
  - Import `motion` and `useReducedMotion` from `motion/react`
  - Props: `children: ReactNode`, `delay?: number` (default 0), `className?: string`
  - Use `whileInView` with `viewport={{ once: true, margin: "-100px" }}`
  - Animation: `opacity: 0 -> 1, y: 24 -> 0` with easeOut curve, duration 0.6s
  - If `useReducedMotion()` is true, set `transition.duration` to 0 (instant)

  **HeroSection** (`hero-section.tsx`):
  - Based on shadcn/studio Pro `hero-section` block (select a variation with centered content)
  - Headline: "Seu consorcio, do sonho a assinatura. Sem formulario." (or similar compelling headline)
  - Sub-headline: "Converse com nosso consultor de IA e receba recomendacoes personalizadas de consorcio em segundos. 100% digital, sem corretor."
  - Primary CTA: `<Link href="/chat">` wrapping a `<Button size="lg">Comecar agora</Button>` styled with primary color
  - Secondary text below CTA: "Gratis, sem compromisso"
  - Mobile-first: text-center on mobile, responsive padding
  - Hero should be full-viewport height (`min-h-[calc(100dvh-4rem)]`) to fill the screen
  - Use brand teal gradient or solid background with contrast
  - Add `pt-20` or similar to account for fixed navbar overlay

  **HowItWorks** (`how-it-works.tsx`):
  - Based on shadcn/studio Pro `bento-grid` or `features-section` block
  - Section heading: "Como funciona"
  - 3 steps with icons (from Lucide):
    1. MessageCircle icon -- "Diga o que quer" / "Conte seu sonho: um carro, imovel, ou servico. Sem formularios."
    2. Search icon -- "Receba recomendacoes" / "Nosso consultor analisa centenas de grupos e encontra o melhor pra voce."
    3. CheckCircle icon -- "Escolha e assine" / "Revise a recomendacao, tire duvidas, e assine. Tudo no chat."
  - Use numbered steps or visual flow indicators
  - Wrap in `<ScrollFade>`

  **BenefitsSection** (`benefits-section.tsx`):
  - Based on shadcn/studio Pro `features-section` block
  - Section heading: "Por que usar o Aja Agora?"
  - 4-6 benefit cards with Lucide icons:
    - Zap: "Rapido" / "Recomendacao em segundos, nao dias"
    - Shield: "Transparente" / "Taxas, custos e simulacoes abertas, sem surpresas"
    - Smartphone: "100% digital" / "Do sonho a assinatura sem sair do celular"
    - Bot: "IA especialista" / "Consultor que entende de consorcio de verdade"
    - DollarSign: "Sem corretor" / "Economize a comissao do intermediario"
    - Clock: "Disponivel 24h" / "Converse quando quiser, sem agendamento"
  - Grid layout: 1 col mobile, 2 cols sm, 3 cols lg
  - Each card wrapped in `<ScrollFade delay={i * 0.1}>`

  **SocialProof** (`social-proof.tsx`):
  - Based on shadcn/studio Pro `testimonials` and/or `social-proof` blocks
  - Section heading: "O que nossos usuarios dizem"
  - 3 placeholder testimonials (placeholder content, clearly marked for future replacement):
    - "Nunca imaginei que comprar um consorcio seria tao simples. Em 5 minutos eu tinha a recomendacao perfeita." -- Maria S., Goiania
    - "O consultor me explicou tudo sobre taxas e prazos. Melhor que qualquer corretor." -- Carlos R., Sao Paulo
    - "Fechei meu consorcio de imovel pelo celular, no sofa de casa. Incrivel." -- Ana P., Brasilia
  - Trust indicators row below testimonials: "100% digital", "Sem corretor", "Analise em segundos" (with appropriate icons)
  - Wrap in `<ScrollFade>`

  **FaqSection** (`faq-section.tsx`):
  - Based on shadcn/studio Pro `faq` block
  - Section heading: "Perguntas frequentes"
  - 4 FAQ items (accordion-style):
    1. "O que e consorcio?" / "Consorcio e uma forma de compra colaborativa onde um grupo de pessoas contribui mensalmente para um fundo comum. Periodicamente, um participante e contemplado e recebe o credito para realizar sua compra."
    2. "O Aja Agora e seguro?" / "Sim. Trabalhamos apenas com administradoras autorizadas pelo Banco Central. Seus dados sao protegidos e nunca compartilhados."
    3. "Preciso pagar para usar?" / "Nao. A consulta e a recomendacao sao 100% gratuitas. Voce so paga quando decidir aderir a um grupo de consorcio."
    4. "Quanto tempo demora para ser contemplado?" / "Depende do grupo e da modalidade (lance ou sorteio). Nosso consultor mostra o historico de contemplacao de cada grupo para voce decidir com informacao."
  - Use shadcn/ui Accordion or the Pro faq block's built-in accordion
  - Wrap in `<ScrollFade>`

  **CtaSection** (`cta-section.tsx`):
  - Based on shadcn/studio Pro `cta-section` block
  - Headline: "Pronto para realizar seu sonho?"
  - Sub-text: "Converse com nosso consultor agora e descubra o melhor consorcio pra voce."
  - CTA button: `<Link href="/chat"><Button size="lg">Comecar agora</Button></Link>`
  - Contrasting background (primary teal or gradient) to stand out
  - Wrap in `<ScrollFade>`

  **Footer** (`footer.tsx`):
  - Based on shadcn/studio Pro `footer` block
  - Brand name "Aja Agora" with tagline
  - Links: "Comecar conversa" (/chat), "Perguntas frequentes" (#faq anchor)
  - Legal text: "Aja Agora nao e uma administradora de consorcio. Atuamos como plataforma de recomendacao. Todos os grupos sao geridos por administradoras autorizadas pelo Banco Central do Brasil."
  - Copyright: "2026 Aja Agora. Todos os direitos reservados."
  - No scroll animation on footer

  **All sections must:**
  - Use Tailwind CSS 4 utility classes, mobile-first
  - Use CSS variable tokens (text-primary, bg-accent, etc.) -- never hardcode colors
  - Use `font-sans` (Geist Sans) for all text
  - Be responsive from 320px to desktop
  - Use `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8` for consistent section padding
  - Have section IDs for potential anchor navigation (e.g., `id="como-funciona"`, `id="faq"`)
  </action>
  <verify>
    <automated>cd /Users/kairo/code/personal/twobrains/aja-agora && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>All 8 section components exist in src/components/landing/, build passes without errors, each section uses brand color tokens</done>
</task>

<task type="auto">
  <name>Task 2: Compose landing page and wire all sections into page.tsx</name>
  <files>src/app/page.tsx</files>
  <action>
  Replace the current redirect in `src/app/page.tsx` with the full landing page composition:

  1. Remove the `redirect("/chat")` import and call
  2. Import all section components from `@/components/landing/`
  3. Import `ScrollFade` wrapper
  4. Compose sections in order per CONTEXT.md:
     - Navbar (will be created in Plan 02, skip import for now -- add a comment placeholder)
     - HeroSection (NO ScrollFade -- above the fold, must render instantly)
     - ScrollFade > HowItWorks
     - ScrollFade > BenefitsSection
     - ScrollFade > SocialProof
     - ScrollFade > FaqSection
     - ScrollFade > CtaSection
     - Footer (no ScrollFade)

  5. Page must be a **Server Component** (no "use client" directive). ScrollFade is a client component imported into a server component -- this is fine in Next.js App Router.

  6. Wrap everything in `<main className="flex flex-col">` with proper section spacing (`space-y-0` -- each section handles its own padding).

  7. Add a comment at the top: `// TODO: Import Navbar from Plan 02`

  The page structure:
  ```tsx
  export default function LandingPage() {
    return (
      <main>
        {/* <Navbar /> -- Plan 02 */}
        <HeroSection />
        <ScrollFade><HowItWorks /></ScrollFade>
        <ScrollFade><BenefitsSection /></ScrollFade>
        <ScrollFade><SocialProof /></ScrollFade>
        <ScrollFade><FaqSection /></ScrollFade>
        <ScrollFade><CtaSection /></ScrollFade>
        <Footer />
      </main>
    );
  }
  ```
  </action>
  <verify>
    <automated>cd /Users/kairo/code/personal/twobrains/aja-agora && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Landing page renders at / with all sections in correct order, no redirect to /chat, CTA buttons link to /chat, build passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Public static landing page, no user input, no API calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Landing page content | accept | All content is hardcoded in JSX -- no injection vector. React auto-escapes. |
| T-06-02 | Information Disclosure | Page source | accept | Public marketing page, no sensitive information to disclose. |
</threat_model>

<verification>
- `curl -s http://localhost:3000 | grep -q "Comecar agora"` -- CTA text present
- `curl -s http://localhost:3000 | grep -q 'href="/chat"'` -- CTA links to chat
- `curl -s http://localhost:3000 | grep -q "Como funciona"` -- How it works section present
- `curl -s http://localhost:3000 | grep -q "Perguntas frequentes"` -- FAQ section present
- `npx next build` succeeds without errors
</verification>

<success_criteria>
1. Landing page loads at `/` with all 7 sections visible on scroll
2. Hero section fills viewport with compelling headline and CTA
3. CTA "Comecar agora" navigates to /chat
4. All sections are responsive on 320px viewport (no horizontal overflow)
5. Scroll animations fade in below-fold sections smoothly
6. All text uses brand teal color scheme (CSS variable tokens)
7. Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-landing-page/06-01-SUMMARY.md`
</output>
