---
phase: 06-landing-page
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/landing/navbar.tsx
  - src/app/layout.tsx
  - src/app/page.tsx
autonomous: true
requirements: [LAND-01, LAND-02, LAND-04]

must_haves:
  truths:
    - "Navbar is fixed at top with brand name and CTA button"
    - "Navbar transitions from transparent to opaque on scroll"
    - "Page metadata includes proper title, description, and OpenGraph tags"
    - "html lang attribute is pt-BR"
    - "Lighthouse mobile performance score is 90+"
    - "Navbar CTA links to /chat"
  artifacts:
    - path: "src/components/landing/navbar.tsx"
      provides: "Fixed navbar with scroll transparency transition"
    - path: "src/app/layout.tsx"
      provides: "Updated metadata and lang attribute"
  key_links:
    - from: "src/components/landing/navbar.tsx"
      to: "/chat"
      via: "next/link CTA"
      pattern: 'href="/chat"'
    - from: "src/app/page.tsx"
      to: "src/components/landing/navbar.tsx"
      via: "import and render"
      pattern: "import.*Navbar"
---

<objective>
Create the navbar component, update SEO metadata, and optimize the landing page for Lighthouse 90+ mobile performance.

Purpose: Complete the landing page with navigation, proper SEO, and ensure the page meets the 90+ Lighthouse performance target.
Output: Navbar component, updated layout.tsx with metadata/lang, page.tsx wired with navbar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-landing-page/06-RESEARCH.md
@.planning/phases/06-landing-page/06-CONTEXT.md
@.planning/phases/03-chat-ui-artifact-rendering/03-UI-SPEC.md
@src/app/layout.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Navbar component with scroll transparency</name>
  <files>src/components/landing/navbar.tsx</files>
  <action>
  **MANDATORY FIRST:** Use shadcn/studio Pro MCP tools to search for navbar blocks:
  1. Call `mcp__shadcn-studio-mcp__get-blocks-metadata` and look for `navbar` blocks
  2. Call `mcp__shadcn-studio-mcp__get-block-meta-content` for navbar variations
  3. Select the most appropriate variation (simple, clean, with CTA button)
  4. Use `/cui` or `/iui` workflow to install/adapt

  If MCP tools are unavailable, build manually following these specs:

  Create `src/components/landing/navbar.tsx`:
  - "use client" directive (needs scroll state)
  - Fixed position at top (`fixed top-0 z-50 w-full`)
  - Scroll behavior: starts transparent (`bg-transparent`), transitions to `bg-background/80 backdrop-blur-md border-b border-border` when `scrollY > 50`
  - Use `useEffect` with passive scroll listener: `window.addEventListener("scroll", handler, { passive: true })`
  - Transition: `transition-all duration-300`
  - Height: consistent 64px (`h-16`)
  - Layout: flex between brand name and CTA
  - Left side: "Aja Agora" text in `font-semibold text-lg`. When transparent, use `text-primary-foreground` or `text-foreground` depending on hero background. When scrolled, use `text-foreground`.
  - Right side: `<Link href="/chat"><Button variant="default" size="sm">Comecar agora</Button></Link>`
  - Mobile: same layout, button may use smaller size
  - Max-width container: `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8`
  - Import `Link` from `next/link`, `Button` from `@/components/ui/button`
  - Use `cn()` from `@/lib/utils` for conditional classes

  **Important:** The navbar text color when transparent depends on the hero background. If hero has a dark/teal background, use white text when transparent. If hero has a light background, use dark text. Check hero-section.tsx implementation from Plan 01 and match accordingly.
  </action>
  <verify>
    <automated>cd /Users/kairo/code/personal/twobrains/aja-agora && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Navbar component renders fixed at top, brand name visible, CTA button links to /chat, scroll transparency transition works</done>
</task>

<task type="auto">
  <name>Task 2: Update metadata/SEO, fix lang attribute, wire navbar into page</name>
  <files>src/app/layout.tsx, src/app/page.tsx</files>
  <action>
  **layout.tsx updates:**

  1. Change `lang="en"` to `lang="pt-BR"` in the html tag -- this is a Brazilian product

  2. Replace the default metadata with SEO-optimized content:
  ```typescript
  export const metadata: Metadata = {
    title: "Aja Agora | Consorcio inteligente com IA",
    description: "Diga o que voce quer e receba uma recomendacao personalizada de consorcio. Sem formulario, sem corretor, 100% digital.",
    keywords: ["consorcio", "consorcio digital", "consorcio IA", "consorcio online", "consorcio sem corretor", "simulacao consorcio"],
    openGraph: {
      title: "Aja Agora | Consorcio inteligente com IA",
      description: "Seu consultor de consorcio com inteligencia artificial. Recomendacoes personalizadas em segundos.",
      type: "website",
      locale: "pt_BR",
    },
    robots: {
      index: true,
      follow: true,
    },
  };
  ```

  3. Verify the body/html classes remain intact (font variables, h-full, antialiased, min-h-full, flex col)

  **page.tsx updates:**

  1. Import `Navbar` from `@/components/landing/navbar`
  2. Remove the `// TODO: Import Navbar from Plan 02` comment if it exists from Plan 01
  3. Add `<Navbar />` as the first child inside `<main>`, before HeroSection
  4. Ensure HeroSection has sufficient top padding (`pt-16` or `pt-20`) to account for the fixed navbar, OR the hero extends behind the transparent navbar (preferred approach -- hero is full-viewport and navbar overlays it)

  **Performance considerations (no extra files, just ensure these patterns):**
  - All landing section components should be Server Components where possible (only scroll-fade.tsx and navbar.tsx need "use client")
  - No unnecessary client-side JS in section components
  - Any images should use `next/image` with explicit dimensions
  - Ensure no large imports are loaded eagerly (Motion is only used in scroll-fade.tsx which is a small client component)
  - The `priority` prop should be on any hero image (if present) to preload it
  </action>
  <verify>
    <automated>cd /Users/kairo/code/personal/twobrains/aja-agora && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
  - html lang is "pt-BR"
  - Page title is "Aja Agora | Consorcio inteligente com IA"
  - Navbar renders at top of landing page
  - Hero section is not obscured by navbar
  - Build passes with zero errors
  - No unnecessary "use client" directives on section components
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Public static navbar and metadata, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | Navbar links | accept | All links are hardcoded (/chat), no dynamic routing or user-controlled URLs |
| T-06-04 | Information Disclosure | SEO metadata | accept | Metadata is intentionally public marketing content |
</threat_model>

<verification>
- `curl -s http://localhost:3000 | grep -q 'lang="pt-BR"'` -- correct language attribute
- `curl -s http://localhost:3000 | grep -q "Consorcio inteligente com IA"` -- SEO title present
- `curl -s http://localhost:3000 | grep -q "Aja Agora"` -- navbar brand name present
- `npx next build` succeeds without errors
- Page has minimal client JS (only navbar + scroll-fade components)
</verification>

<success_criteria>
1. Navbar fixed at top with "Aja Agora" brand and "Comecar agora" CTA to /chat
2. Navbar transitions from transparent to opaque with backdrop-blur on scroll
3. html lang="pt-BR" set correctly
4. Metadata includes title, description, keywords, OpenGraph tags
5. Build passes, no unnecessary "use client" directives
6. Page optimized for Lighthouse 90+ mobile (Server Components, minimal client JS, optimized images)
</success_criteria>

<output>
After completion, create `.planning/phases/06-landing-page/06-02-SUMMARY.md`
</output>
