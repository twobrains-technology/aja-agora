---
phase: 11-dashboard-funnel-analytics
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/components/ui/chart.tsx
  - src/components/admin/dashboard/date-range-filter.tsx
  - src/components/admin/dashboard/kpi-cards.tsx
  - src/components/admin/dashboard/funnel-chart.tsx
  - src/components/admin/dashboard/lead-volume-chart.tsx
  - src/components/admin/dashboard/channel-breakdown-chart.tsx
  - src/app/admin/(dashboard)/page.tsx
autonomous: false
requirements:
  - BACK-10
  - BACK-11

must_haves:
  truths:
    - "Admin sees 4 KPI cards with values and trend arrows at the top of /admin"
    - "Admin sees a horizontal funnel chart showing conversion between stages"
    - "Admin sees an area chart of lead volume over time"
    - "Admin sees a donut chart of channel distribution"
    - "Changing date range filter updates all metrics"
    - "Dashboard is usable on mobile (stacked layout)"
  artifacts:
    - path: "src/components/admin/dashboard/kpi-cards.tsx"
      provides: "4 KPI cards with trend indicators"
    - path: "src/components/admin/dashboard/funnel-chart.tsx"
      provides: "CSS-based horizontal funnel visualization"
    - path: "src/components/admin/dashboard/lead-volume-chart.tsx"
      provides: "recharts AreaChart for daily lead volume"
    - path: "src/components/admin/dashboard/channel-breakdown-chart.tsx"
      provides: "recharts PieChart donut for channel distribution"
    - path: "src/components/admin/dashboard/date-range-filter.tsx"
      provides: "Date range picker with nuqs URL state"
    - path: "src/app/admin/(dashboard)/page.tsx"
      provides: "Dashboard page composing all components"
  key_links:
    - from: "src/app/admin/(dashboard)/page.tsx"
      to: "/api/admin/dashboard"
      via: "fetch with from/to params"
      pattern: "api/admin/dashboard"
    - from: "src/components/admin/dashboard/date-range-filter.tsx"
      to: "nuqs"
      via: "useQueryState for from/to URL params"
      pattern: "parseAsIsoDate"
---

<objective>
Build all dashboard UI components and assemble them into the /admin page, replacing the current placeholder.

Purpose: This is the visual layer — KPI cards, CSS funnel, recharts timeline, recharts donut, date filter — all wired to the API from Plan 01.
Output: Fully functional analytics dashboard at /admin with responsive layout and interactive charts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-dashboard-funnel-analytics/11-CONTEXT.md
@.planning/phases/11-dashboard-funnel-analytics/11-RESEARCH.md
@.planning/phases/11-dashboard-funnel-analytics/11-01-SUMMARY.md

@src/app/admin/(dashboard)/page.tsx
@src/app/admin/(dashboard)/layout.tsx
@src/lib/admin/dashboard-types.ts
@src/components/admin/pipeline/pipeline-filters.tsx

<interfaces>
<!-- From Plan 01 — types the executor needs -->

From src/lib/admin/dashboard-types.ts:
```typescript
export interface KpiData {
  totalLeads: number;
  leadsToday: number;
  avgFunnelDays: number;
  conversionRate: number;
  trends: {
    totalLeads: number;
    leadsToday: number;
    avgFunnelDays: number;
    conversionRate: number;
  };
}

export interface FunnelStage {
  stage: string;
  label: string;
  count: number;
  percentOfTotal: number;
  dropOffRate: number;
}

export interface DailyVolume {
  date: string;
  count: number;
}

export interface ChannelBreakdown {
  channel: "web" | "whatsapp";
  count: number;
  percent: number;
}

export interface DashboardResponse {
  kpis: KpiData;
  funnel_stages: FunnelStage[];
  daily_volume: DailyVolume[];
  channel_breakdown: ChannelBreakdown[];
}

export const FUNNEL_STAGES: { stage: string; label: string }[];
```

From src/components/admin/pipeline/pipeline-filters.tsx (existing nuqs pattern):
```typescript
// Uses useQueryState with parseAsIsoDate from nuqs
// Uses shadcn Calendar + Popover for date picking
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install recharts via shadcn chart + create date range filter</name>
  <files>src/components/ui/chart.tsx, src/components/admin/dashboard/date-range-filter.tsx</files>
  <read_first>src/components/admin/pipeline/pipeline-filters.tsx, src/app/admin/(dashboard)/layout.tsx</read_first>
  <action>
**Step 1: Install shadcn chart component** (per D-12, research finding #1):
```bash
npx shadcn@latest add chart
```
This installs `recharts@3.8.x` and creates `src/components/ui/chart.tsx` with `ChartContainer`, `ChartTooltip`, `ChartTooltipContent`, `ChartLegend`.

**Step 2: Create `src/components/admin/dashboard/date-range-filter.tsx`** (per D-03):

A `"use client"` component. Follow the existing pattern from `pipeline-filters.tsx` for nuqs usage.

Props: none (manages own URL state).

Implementation:
1. Use `useQueryState("from", parseAsIsoDate.withDefault(subDays(new Date(), 30)))` and `useQueryState("to", parseAsIsoDate.withDefault(new Date()))` from nuqs.
2. Render two date pickers side-by-side using shadcn `Popover` + `Calendar` (same pattern as pipeline-filters.tsx).
3. Label: "Periodo:" with from/to date pickers showing formatted date (format "dd/MM/yyyy" using date-fns `format`).
4. Layout: `flex items-center gap-2` — compact enough for top-right placement.
5. Include a "Ultimos 30 dias" quick-select button that resets both to defaults.

Export as named export `DateRangeFilter`.
  </action>
  <verify>
    <automated>cd /Users/kairo/code/personal/twobrains/aja-agora && test -f src/components/ui/chart.tsx && grep -q "parseAsIsoDate" src/components/admin/dashboard/date-range-filter.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- File src/components/ui/chart.tsx exists (shadcn chart installed)
- grep "ChartContainer" src/components/ui/chart.tsx returns a match
- grep "use client" src/components/admin/dashboard/date-range-filter.tsx returns a match
- grep "parseAsIsoDate" src/components/admin/dashboard/date-range-filter.tsx returns a match
- grep "DateRangeFilter" src/components/admin/dashboard/date-range-filter.tsx returns a match
- package.json contains "recharts" dependency
  </acceptance_criteria>
  <done>recharts installed via shadcn chart, DateRangeFilter component with nuqs URL state and Calendar pickers ready</done>
</task>

<task type="auto">
  <name>Task 2: Build KPI cards, funnel chart, recharts charts, and assemble dashboard page</name>
  <files>src/components/admin/dashboard/kpi-cards.tsx, src/components/admin/dashboard/funnel-chart.tsx, src/components/admin/dashboard/lead-volume-chart.tsx, src/components/admin/dashboard/channel-breakdown-chart.tsx, src/app/admin/(dashboard)/page.tsx</files>
  <read_first>src/lib/admin/dashboard-types.ts, src/components/ui/chart.tsx, src/components/admin/dashboard/date-range-filter.tsx</read_first>
  <action>
**Create 4 dashboard components + assemble the page.**

**Component 1: `src/components/admin/dashboard/kpi-cards.tsx`** (per D-04, D-05, D-06):

`"use client"` component. Props: `{ kpis: KpiData }`.

Render a grid of 4 cards using shadcn `Card`/`CardHeader`/`CardContent`:
1. **Total Leads**: `kpis.totalLeads`, icon: `Users` from lucide-react
2. **Leads Hoje**: `kpis.leadsToday`, icon: `UserPlus`
3. **Tempo Medio no Funil**: `${kpis.avgFunnelDays} dias`, icon: `Clock`
4. **Taxa de Conversao**: `${kpis.conversionRate.toFixed(1)}%`, icon: `TrendingUp`

Each card shows:
- Small icon + label in CardHeader (text-sm font-medium text-muted-foreground)
- Large value in CardContent (text-2xl font-bold)
- Trend line below value: `TrendingUp` or `TrendingDown` icon from lucide + `${Math.abs(trend)}% vs periodo anterior`. Green text (text-green-600) for positive trends, red (text-red-600) for negative. For avgFunnelDays, INVERT the color logic (lower is better — negative trend = green).

Grid layout: `grid grid-cols-2 lg:grid-cols-4 gap-4`.

**Component 2: `src/components/admin/dashboard/funnel-chart.tsx`** (per D-07, D-08, D-09, D-10):

`"use client"` component. Props: `{ stages: FunnelStage[] }`.

CSS-only horizontal funnel with trapezoid/chevron shapes:
1. Wrap in a shadcn `Card` with title "Funil de Conversao".
2. Use a `flex items-center gap-1` container.
3. Each stage is a div with:
   - `clip-path: polygon(0 0, calc(100% - 12px) 0, 100% 50%, calc(100% - 12px) 100%, 0 100%, 12px 50%)` for chevron shape. First item: no left indent (`polygon(0 0, calc(100% - 12px) 0, 100% 50%, calc(100% - 12px) 100%, 0 100%, 0 0)`).
   - Width proportional to count relative to first stage: `width: ${Math.max((stage.count / stages[0].count) * 100, 15)}%` — min 15% so empty stages are still visible.
   - Background color: use a predefined color array per stage index (e.g., blue-500, cyan-500, teal-500, green-500, emerald-500, lime-500). Use Tailwind bg classes.
   - Inside each chevron: stage label (text-xs), count (font-bold), and percentage (text-xs opacity-80 showing `${percentOfTotal.toFixed(0)}%`).
4. Below the funnel, show drop-off rates between stages as small text labels.
5. Handle empty state: if all counts are 0, show "Sem dados no periodo" message.
6. On mobile (below md breakpoint): stack vertically instead of horizontal, each stage full-width with height proportional instead of width.

**Component 3: `src/components/admin/dashboard/lead-volume-chart.tsx`** (per D-11, D-12, D-13):

`"use client"` component. Props: `{ data: DailyVolume[] }`.

1. Wrap in a shadcn `Card` with title "Volume de Leads".
2. Use `ChartContainer` from shadcn chart with config: `{ leads: { label: "Leads", color: "var(--chart-1)" } }`.
3. Inside: recharts `AreaChart` with `data` prop.
4. Add `CartesianGrid` with `strokeDasharray="3 3"`.
5. `XAxis` with `dataKey="date"`, format tick labels to "dd/MM" using date-fns `format`.
6. `YAxis` with `allowDecimals={false}`.
7. `ChartTooltip` with `ChartTooltipContent` for hover tooltip showing exact count (per D-13).
8. `Area` with `type="monotone"`, `dataKey="count"`, `fill="var(--color-leads)"`, `stroke="var(--color-leads)"`, `fillOpacity={0.3}`.
9. Height: `className="h-[300px]"`.
10. Empty state: if data is empty or all counts 0, show "Sem dados no periodo" instead of chart.

**Component 4: `src/components/admin/dashboard/channel-breakdown-chart.tsx`** (per D-14, D-15, D-16):

`"use client"` component. Props: `{ data: ChannelBreakdown[] }`.

1. Wrap in a shadcn `Card` with title "Canais".
2. Use `ChartContainer` with config for web and whatsapp colors.
3. Inside: recharts `PieChart` with `Pie` using `innerRadius={60}` for donut effect (per D-14).
4. `dataKey="count"`, `nameKey="channel"`.
5. Use `Cell` components with distinct colors per channel (e.g., blue for web, green for whatsapp).
6. Add a custom center label showing total count.
7. Add `ChartTooltip` showing channel name, count, and percentage (per D-16).
8. Below the donut, show a legend: colored dot + channel name + count + percentage.
9. Height: `className="h-[300px]"`.
10. Empty state: if no data, show "Sem dados".

**Assemble: `src/app/admin/(dashboard)/page.tsx`** (per D-01, D-02):

Replace the current placeholder with a `"use client"` component that:

1. Uses `useQueryState` for `from` and `to` with `parseAsIsoDate` (same defaults as DateRangeFilter — last 30 days).
2. Fetches data from `/api/admin/dashboard?from=${from.toISOString()}&to=${to.toISOString()}` using `useEffect` + `fetch`. Refetch when from/to change.
3. Shows loading state with `Skeleton` components matching the layout.

Layout structure (per D-02):
```
<header>  "Dashboard" title + DateRangeFilter at top-right  </header>
<KpiCards />                    <!-- full width, 4-col grid -->
<FunnelChart />                 <!-- full width -->
<div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
  <LeadVolumeChart />           <!-- left -->
  <ChannelBreakdownChart />     <!-- right -->
</div>
```

Gap between sections: `space-y-6`.
Mobile: all sections stack vertically (grid-cols-1 on small screens).
  </action>
  <verify>
    <automated>cd /Users/kairo/code/personal/twobrains/aja-agora && grep -q "KpiCards" src/app/admin/\(dashboard\)/page.tsx && grep -q "FunnelChart" src/app/admin/\(dashboard\)/page.tsx && grep -q "LeadVolumeChart" src/app/admin/\(dashboard\)/page.tsx && grep -q "ChannelBreakdownChart" src/app/admin/\(dashboard\)/page.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- grep "KpiCards" src/app/admin/\(dashboard\)/page.tsx returns a match
- grep "FunnelChart" src/app/admin/\(dashboard\)/page.tsx returns a match
- grep "LeadVolumeChart" src/app/admin/\(dashboard\)/page.tsx returns a match
- grep "ChannelBreakdownChart" src/app/admin/\(dashboard\)/page.tsx returns a match
- grep "DateRangeFilter" src/app/admin/\(dashboard\)/page.tsx returns a match
- grep "clip-path" src/components/admin/dashboard/funnel-chart.tsx returns a match
- grep "AreaChart" src/components/admin/dashboard/lead-volume-chart.tsx returns a match
- grep "PieChart" src/components/admin/dashboard/channel-breakdown-chart.tsx returns a match
- grep "innerRadius" src/components/admin/dashboard/channel-breakdown-chart.tsx returns a match
- grep "TrendingUp\|TrendingDown" src/components/admin/dashboard/kpi-cards.tsx returns matches
- grep "api/admin/dashboard" src/app/admin/\(dashboard\)/page.tsx returns a match
- grep "grid-cols-2.*lg:grid-cols-4" src/components/admin/dashboard/kpi-cards.tsx returns a match (responsive grid)
  </acceptance_criteria>
  <done>All 4 dashboard visualization components created and assembled into the /admin page with responsive layout, loading skeletons, and empty states</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete dashboard visually</name>
  <files>src/app/admin/(dashboard)/page.tsx</files>
  <action>
Human verifies the complete analytics dashboard at /admin.

What was built: KPI cards (4 metrics + trend arrows), CSS funnel chart (horizontal chevrons, no "perdido"), lead volume area chart (recharts), channel donut chart (recharts), and date range filter affecting all metrics.

Steps to verify:
1. Start the dev server: `npm run dev`
2. Navigate to http://localhost:3000/admin/login and log in
3. You should land on the dashboard at /admin
4. Verify 4 KPI cards at top: Total Leads, Leads Hoje, Tempo Medio no Funil, Taxa de Conversao — each with trend arrow
5. Verify horizontal funnel chart below KPIs showing stages as colored chevrons (Novo -> Engajado -> Qualificado -> Em Negociacao -> Proposta Enviada -> Fechado Ganho). "Perdido" should NOT appear.
6. Verify area chart (Volume de Leads) on the left below the funnel
7. Verify donut chart (Canais) on the right showing Web vs WhatsApp
8. Change the date range filter — all charts and KPIs should update
9. Check mobile view (resize to ~375px width) — everything stacks vertically
10. If no leads exist, verify empty states show "Sem dados" messages instead of broken charts
  </action>
  <verify>Human confirms: type "approved" or describe issues</verify>
  <done>Dashboard visually verified by human — all sections render correctly, responsive layout works, date filter updates all metrics</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> page | Dashboard page is behind admin auth (layout enforces login) |
| page -> API | Fetch to /api/admin/dashboard with date params |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Spoofing | Dashboard page access | mitigate | Admin layout requires auth session; page fetches from auth-gated API |
| T-11-05 | Information Disclosure | Chart data | accept | Shows aggregate counts only, no PII. Auth-gated. |
</threat_model>

<verification>
1. Dashboard at /admin renders all 4 sections (KPIs, funnel, timeline, channel)
2. Date range filter changes update all visualizations
3. Funnel shows 6 stages (no "perdido")
4. Charts handle empty data gracefully
5. Mobile layout stacks all sections vertically
6. No console errors from recharts SSR (all chart components are "use client")
</verification>

<success_criteria>
- /admin shows complete dashboard replacing the placeholder
- 4 KPI cards with values and trend arrows
- CSS funnel chart with chevron shapes and conversion percentages
- recharts area chart for daily lead volume
- recharts donut chart for channel breakdown
- Date range filter updates all metrics via nuqs URL state
- Responsive layout works on mobile
- Empty states handle zero-data gracefully
</success_criteria>

<output>
After completion, create `.planning/phases/11-dashboard-funnel-analytics/11-02-SUMMARY.md`
</output>
