---
phase: 09-lead-pipeline-kanban
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/app/admin/(dashboard)/pipeline/page.tsx
  - src/components/admin/pipeline/kanban-board.tsx
  - src/components/admin/pipeline/kanban-column.tsx
  - src/components/admin/pipeline/lead-card.tsx
  - src/components/admin/pipeline/pipeline-filters.tsx
autonomous: true
requirements: [BACK-04, BACK-05, BACK-06]
must_haves:
  truths:
    - "Kanban board at /admin/pipeline shows 7 columns for funnel stages"
    - "Lead cards display name, channel icon, time in stage, credit value"
    - "Dragging a card between columns updates stage via API and shows optimistic UI"
    - "Filters by channel, date range, and text search work and persist in URL"
    - "Board auto-refreshes every 30 seconds via polling"
    - "Column headers show lead count badges"
  artifacts:
    - path: "src/components/admin/pipeline/kanban-board.tsx"
      provides: "DragDropContext with columns, polling, optimistic updates"
      contains: "DragDropContext"
    - path: "src/components/admin/pipeline/kanban-column.tsx"
      provides: "Droppable column with header + count badge"
      contains: "Droppable"
    - path: "src/components/admin/pipeline/lead-card.tsx"
      provides: "Draggable lead card with summary info"
      contains: "Draggable"
    - path: "src/components/admin/pipeline/pipeline-filters.tsx"
      provides: "Filter bar with channel, date range, text search via nuqs"
      contains: "useQueryState"
  key_links:
    - from: "src/components/admin/pipeline/kanban-board.tsx"
      to: "/api/admin/leads"
      via: "fetch in useEffect + polling interval"
      pattern: "fetch.*api/admin/leads"
    - from: "src/components/admin/pipeline/kanban-board.tsx"
      to: "/api/admin/leads/[id]/stage"
      via: "fetch PATCH on drag end"
      pattern: "fetch.*stage"
---

<objective>
Build the Kanban board UI with drag-and-drop between funnel stages, lead cards with summary info, client-side filters with URL state, and 30-second polling.

Purpose: Core pipeline management interface for admins to visualize and manage the lead funnel.
Output: Fully interactive Kanban board at /admin/pipeline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-lead-pipeline-kanban/09-CONTEXT.md
@.planning/phases/09-lead-pipeline-kanban/09-RESEARCH.md
@.planning/phases/09-lead-pipeline-kanban/09-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — src/lib/admin/lead-transitions.ts -->
```typescript
export const STAGE_ORDER = ["novo","engajado","qualificado","em_negociacao","proposta_enviada","fechado_ganho","perdido"] as const;
export type LeadStage = typeof STAGE_ORDER[number];
```

<!-- From Plan 01 — GET /api/admin/leads response shape -->
```typescript
// Response: { leads: Record<LeadStage, Lead[]>, stages: typeof STAGE_ORDER }
// Each Lead: { id, conversationId, name, phone, email, stage, creditValue, createdAt, updatedAt, conversation: { channel, createdAt, updatedAt } }
```

<!-- From Plan 01 — PATCH /api/admin/leads/[id]/stage -->
```typescript
// Request body: { stage: LeadStage }
// Response: updated lead object
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Kanban board with DnD, lead cards, and polling</name>
  <files>src/app/admin/(dashboard)/pipeline/page.tsx, src/components/admin/pipeline/kanban-board.tsx, src/components/admin/pipeline/kanban-column.tsx, src/components/admin/pipeline/lead-card.tsx</files>
  <read_first>src/app/admin/(dashboard)/pipeline/page.tsx, src/db/schema.ts, .planning/phases/09-lead-pipeline-kanban/09-01-SUMMARY.md</read_first>
  <action>
1. Create `src/components/admin/pipeline/lead-card.tsx` ("use client"):
   - Props: `lead: { id, name, phone, email, stage, creditValue, createdAt, updatedAt, conversation: { channel, createdAt, updatedAt } }`, `isDragging: boolean`
   - Render a shadcn Card with:
     - Lead name (or phone if name is null, or "Lead sem nome")
     - Channel icon: Globe from lucide-react for "web", Smartphone for "whatsapp" — small badge-style
     - Time in current stage: use `formatDistanceToNow` from `date-fns` with `{ addSuffix: true, locale: ptBR }`. Import `ptBR` from `date-fns/locale/pt-BR`. Calculate from `lead.updatedAt` (stage change updates this).
     - Credit value: format as BRL currency if present, otherwise show "—". Use `Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' })`.
     - Last interaction: relative time from `lead.conversation.updatedAt`
   - On click: show a toast "Em breve: detalhes da conversa" (per D-05). Import `toast` from sonner if available, or use a simple alert. Check if sonner is installed; if not, use `window.alert` or install sonner.
   - Apply `opacity-50` when `isDragging` for visual feedback
   - Card should be compact — suitable for a Kanban column

2. Create `src/components/admin/pipeline/kanban-column.tsx` ("use client"):
   - Props: `stage: string`, `leads: Lead[]`, `label: string`
   - Use `Droppable` from `@hello-pangea/dnd` with `droppableId={stage}`
   - Column header: stage label (human-readable), count badge showing `leads.length` (per D-07)
   - Human-readable labels map: `{ novo: "Novo", engajado: "Engajado", qualificado: "Qualificado", em_negociacao: "Em Negociacao", proposta_enviada: "Proposta Enviada", fechado_ganho: "Fechado Ganho", perdido: "Perdido" }`
   - Stage color accents: "fechado_ganho" gets green accent (border-green-500 or bg-green-50), "perdido" gets muted gray (bg-muted), others get subtle neutral styling
   - Each lead rendered inside a `Draggable` with `draggableId={lead.id}` and `index`
   - Empty state: show a muted text "Nenhum lead" when column is empty
   - Column should scroll vertically if many leads (max-h with overflow-y-auto)

3. Create `src/components/admin/pipeline/kanban-board.tsx` ("use client"):
   - Import `DragDropContext` from `@hello-pangea/dnd`
   - Import `STAGE_ORDER` type from `@/lib/admin/lead-transitions` (the const array — shared between frontend and backend)
   - State: `columns: Record<string, Lead[]>` initialized empty
   - State: `loading: boolean` for initial load
   - Fetch leads on mount: `GET /api/admin/leads` → populate columns state
   - Polling: `useEffect` with `setInterval` every 30 seconds (per D-16). Use `useRef` for the interval to prevent stale closures. Clear on unmount.
   - `onDragEnd(result: DropResult)`:
     - If no destination or same position, return
     - Optimistic update: clone columns via `structuredClone`, splice source, insert at destination, update state (per D-17)
     - Save previous state for rollback
     - `fetch(`/api/admin/leads/${draggableId}/stage`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ stage: destination.droppableId }) })`
     - On error: revert to saved state, optionally show toast
   - Render: horizontal flex container with `ScrollArea` for mobile horizontal scroll. Each stage in `STAGE_ORDER` renders a `KanbanColumn`.
   - Loading state: show Skeleton cards in each column
   - Accept `filterFn` prop: `(lead: Lead) => boolean` — applied when rendering columns so filters work without refetching
   - Expose a `refreshLeads` function via ref or callback for the filter component to trigger manual refresh

4. Replace `src/app/admin/(dashboard)/pipeline/page.tsx`:
   - Server component that imports the client `KanbanBoard`
   - Page header: "Pipeline" title, brief description
   - Render `<KanbanBoard />` (filters integrated within the board or as sibling — see Task 2)
  </action>
  <verify>
    <automated>cd /Users/kairo/code/personal/twobrains/aja-agora && test -f src/components/admin/pipeline/kanban-board.tsx && test -f src/components/admin/pipeline/kanban-column.tsx && test -f src/components/admin/pipeline/lead-card.tsx && grep -q 'DragDropContext' src/components/admin/pipeline/kanban-board.tsx && grep -q 'Droppable' src/components/admin/pipeline/kanban-column.tsx && grep -q 'Draggable' src/components/admin/pipeline/kanban-column.tsx && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `kanban-board.tsx` uses DragDropContext, fetches from /api/admin/leads, polls every 30s
    - `kanban-column.tsx` uses Droppable, shows stage label + count badge, has green accent for fechado_ganho and muted for perdido
    - `lead-card.tsx` shows name, channel icon (Globe/Smartphone), time in stage (formatDistanceToNow), credit value (BRL), last interaction
    - lead-card.tsx shows toast on click (per D-05)
    - Cards ordered by most recent first within columns (per D-06)
    - onDragEnd does optimistic update + PATCH + revert on error (per D-03, D-17)
    - Horizontal ScrollArea wraps columns for mobile (per mobile-first constraint)
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Kanban board renders 7 columns with draggable lead cards, optimistic DnD updates, 30s polling, mobile scroll, and proper card info display.</done>
</task>

<task type="auto">
  <name>Task 2: Build filter bar with nuqs URL state</name>
  <files>src/components/admin/pipeline/pipeline-filters.tsx</files>
  <read_first>src/components/admin/pipeline/kanban-board.tsx</read_first>
  <action>
1. Create `src/components/admin/pipeline/pipeline-filters.tsx` ("use client"):
   - Import `useQueryState`, `parseAsStringEnum`, `parseAsString`, `parseAsIsoDate` from `nuqs` (per D-20)
   - Channel filter: `useQueryState("channel", parseAsStringEnum(["all", "web", "whatsapp"] as const).withDefault("all"))`
   - Text search: `useQueryState("q", parseAsString.withDefault(""))`
   - Date range: `useQueryState("from", parseAsIsoDate)` and `useQueryState("to", parseAsIsoDate)`
   - Render filter bar (per D-18):
     - shadcn `Select` for channel: options "Todos", "Web", "WhatsApp"
     - shadcn `Input` with Search icon for text search (debounce 300ms with setTimeout/clearTimeout)
     - shadcn `Popover` + `Calendar` for date range: "De" and "Ate" date pickers. If `parseAsIsoDate` is not available, use `parseAsString` with ISO date strings.
   - Export a `useLeadFilters()` hook that returns `{ channel, search, dateFrom, dateTo, filterFn }` where `filterFn: (lead: Lead) => boolean`:
     - Channel: if not "all", match `lead.conversation.channel`
     - Search: if not empty, match against `lead.name` or `lead.phone` (case-insensitive includes)
     - Date range: if set, match `lead.createdAt` within range
     - All filters are AND-combined (per D-19)

2. Wire into `kanban-board.tsx`:
   - Import and render `<PipelineFilters />` above the columns
   - Use `useLeadFilters()` hook to get `filterFn`
   - Apply `filterFn` when rendering: `columns[stage].filter(filterFn)`
   - The filter is client-side only — no server round-trip (per D-19)

3. Ensure polling does not reset filter state. Use `useRef` for filter values in the polling callback, or ensure filters are URL-derived (nuqs handles this automatically since state is in URL, not React state).
  </action>
  <verify>
    <automated>cd /Users/kairo/code/personal/twobrains/aja-agora && test -f src/components/admin/pipeline/pipeline-filters.tsx && grep -q 'useQueryState' src/components/admin/pipeline/pipeline-filters.tsx && grep -q 'parseAsString' src/components/admin/pipeline/pipeline-filters.tsx && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pipeline-filters.tsx` uses nuqs `useQueryState` for channel, search, and date range
    - Filter state persists in URL search params (shareable filtered views per D-20)
    - Channel dropdown uses shadcn Select with "Todos", "Web", "WhatsApp" options
    - Text search filters by name or phone (case-insensitive)
    - Date range uses Popover + Calendar components
    - Filters apply client-side without refetching (per D-19)
    - Polling does not reset active filters
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Filter bar with channel, text search, and date range. Filters persist in URL. Client-side filtering on lead data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response -> client state | Lead data from API populates client UI; no PII exposure risk beyond admin access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-09-05 | Tampering | Client-side optimistic update | accept | API validates; client reverts on error |
| T-09-06 | Information Disclosure | Lead PII in client state | accept | Admin users are authorized to see all lead data |
</threat_model>

<verification>
1. `npm run build` succeeds
2. All 5 component files exist
3. Kanban board renders at /admin/pipeline with 7 columns
4. Drag and drop updates lead stage
5. Filters persist in URL and apply correctly
</verification>

<success_criteria>
- Kanban board shows 7 stage columns with lead cards
- Cards display name, channel icon, time in stage, credit value
- Drag-and-drop works with optimistic UI and API persistence
- Filters by channel, text search, and date range work client-side
- Filter state persists in URL via nuqs
- Board polls every 30 seconds
- Mobile horizontal scroll works
</success_criteria>

<output>
After completion, create `.planning/phases/09-lead-pipeline-kanban/09-02-SUMMARY.md`
</output>
