# QA Noturno — Ledger · 2026-06-21 20:17

> Branch de trabalho: `feat/mesa-copiloto` (== `develop` após FF de `2bd46d01`).
> Ancoragem: merge pra develop = **fundação da mesa** (schema + migration 0026, `31bbb8a2`) +
> **bloco copiloto** (FIX-66 roteamento `d038cbf4` + FIX-67 agente `ce97c354`).
> Objetivo: validar a superfície recém-mergeada no nível certo (§5: agente→eval/cassette,
> DB/IO→integration, estrutural→unit), achar bugs, corrigir e revalidar até verde.
> Ambiente: stack do workspace de pé (`aja-app/pg-mesa-copiloto`, bind mount deste worktree).

## Natureza da superfície
O bloco copiloto é **backend de canal WhatsApp** (inbound roteado por número) + agente LLM.
**NÃO tem superfície de browser** — então E2E Playwright não é o nível certo (§5 da skill).
Os produtores do journey completo (CRUD de administradora/docs/atendente = mesa-a; transbordo
no kanban = mesa-b) **ainda não estão em develop** → o journey ponta-a-ponta
(kanban→transbordo→copiloto) **não é testável agora**; validei meu slice com fixtures semeadas
que simulam o que mesa-a/mesa-b vão produzir (cenário 9).

## Cenários

| # | Cenário (fluxo) | Origem | Tipo | Status | Bug card | Último resultado |
|---|-----------------|--------|------|--------|----------|------------------|
| 1 | Builder injeta o PDF (texto_extraido) da administradora certa no system prompt | ce97c354 | structural+cassette | ✅ | — | 13 + cassette verdes |
| 2 | Copiloto orienta o ATENDENTE, não vaza stack/meta, responde PT-BR | ce97c354 | cassette+eval+smoke | ✅ | — | cassette + eval LLM real + smoke real verdes |
| 3 | Roteamento: número de mesa → copiloto, NUNCA vendas (precedência §8) | d038cbf4 | structural+integration | ✅ | — | processor.test + cassette routing verdes |
| 4 | Persiste attendant+assistant em mesa_copilot_messages (DB real) | d038cbf4 | integration | ✅ | — | routing.integration 6 verdes |
| 5 | Sem handoff aberto → ack, não chama LLM, não cai em vendas | d038cbf4 | integration | ✅ | — | verde |
| 6 | **Reply longo/markdown → split (≤4096) + formato WhatsApp antes de enviar** | NOVO (achado QA) | integration | ✅ fechado | done/bug-copiloto-sem-split-format.md | test+fix `785768b4` — 8 integration verdes |
| 7 | Migration 0026 aplicada sem drift nas 5 tabelas mesa | 31bbb8a2 | schema | ✅ | — | 5 tabelas aplicadas; collision 0011-0013 é pré-existente, alheio |
| 8 | Regressão: hook no processor.ts não quebra fluxo de vendas/web | d038cbf4 | unit | ✅ | — | test:unit 1832 verde + processor.test 7 verde |
| 9 | Cobertura de escopo: journey E2E kanban→transbordo→copiloto | — | — | ⚠️ bloqueado | — | depende de mesa-a (CRUDs) + mesa-b (transbordo), NÃO em develop |

## Log de transições
- 20:17 — Ledger criado. Anchored a superfície do merge pra develop.
- 20:17 — Cenários 1-5,8 já verdes (validação do bloco). Cenário 7 verde (schema aplicado).
- 20:18 — **Achado adversarial (cenário 6):** `handleMesaCopilot` envia o reply do LLM com
  `sendTextMessage` cru — sem `formatTextForWhatsApp` nem `splitMessage(4096)` que o caminho de
  vendas (`adapter.ts`) aplica. Reply passo-a-passo do manual passa de 4096 (Meta rejeita) e o
  markdown (`##`/`**`) do LLM aparece cru pro atendente. Bug real na superfície. → TDD inline.
- 20:19 — TDD: 2 testes de regressão falhando (1 chunk só; `##` cru) → fix em `routing.ts`
  (formata + divide, persiste reply cru) → 8 integration verdes. test:unit 1832 verde
  (zero regressão). Commit `test+fix: 785768b4`. Cenário 6 ✅.
- 20:20 — **Probes adversariais adicionais** (sem novo bug): reply vazio (edge raro, mesma
  exposição do caminho de vendas — fora de escopo endurecer aqui); cache de atendente
  (`invalidateMesaAttendantCache` exportado p/ o CRUD do mesa-a chamar, como o de vendas);
  múltiplos handoffs (pega o mais recente aberto); migration 0026 (5 tabelas aplicadas; a
  collision do `drizzle-kit check` é em 0011-0013, **pré-existente**, alheia à 0026).
- 20:21 — **Parada (§8):** todos os cenários ✅ exceto o 9 (⚠️ bloqueado — escopo). Sem bloco
  pendente. Sem commit novo pra ancorar. Critério de PRONTO atingido.

## Achado de COBERTURA DE ESCOPO (reportar ao Kairo — cenário 9)
O bloco copiloto (FIX-66/67) está **integrado e validado em isolamento** (agente, roteamento,
persistência — fixtures semeadas simulam o estado que os blocos irmãos produzem). Mas o
**journey ponta-a-ponta NÃO é testável ainda** porque os produtores não estão em develop:
- **mesa-a** (CRUD de Administradora + Documentos + Atendente de mesa no admin) — sem ele não
  há como cadastrar a administradora/PDF/atendente pela UI (o copiloto consome esses dados).
- **mesa-b** (botão "Transbordar para a mesa" no kanban → cria `mesa_handoffs`) — sem ele não
  há handoff aberto vindo do fluxo real.
Quando mesa-a e mesa-b entrarem em develop, o journey **kanban → transbordo → WhatsApp do
atendente → copiloto** vira testável de verdade (E2E admin no browser + WhatsApp inbound). Até
lá, não é gap do MEU bloco — é sequência de ondas. **Decisão de negócio do Kairo:** validar o
journey completo na onda que fechar mesa-a/mesa-b.

## Resultado final
- **8 cenários ✅** (1-8), **1 bug achado→corrigido** (split/format, `785768b4`), **0 bloco pendente**.
- **1 achado de escopo** (cenário 9): journey E2E depende de mesa-a/mesa-b — reportado.
- Evidência fresca: test:unit **1832 verde**, integration mesa **8 verde**, Camada 3 eval real
  verde, smoke real do copiloto verde.
