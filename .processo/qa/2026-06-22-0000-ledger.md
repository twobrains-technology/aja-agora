# QA Noturno — ledger · transbordo → mesa administradora → WhatsApp → orientar operador · 2026-06-22

- **Branch de trabalho (qa-base):** `qa-base/2026-06-22-transbordo-mesa-whatsapp` (fork de `develop`)
- **Stack:** `aja-app-develop` (Up, bind `/Users/kairo/code/aja-agora` → `/app`, reiniciado limpo na qa-base). Domínio: `http://aja-develop.orb.local`.
- **Objetivo (1 frase):** Validar E2E a COERÊNCIA do transbordo do kanban → mesa administradora → recebimento no WhatsApp do atendente → copiloto orientando o operador, corrigir o que falhar, deixar verde na qa-base.
- **Critério de PRONTO:** todos os cenários T1–T9 ∈ {✅ | ✅ decidido+anotado} + gate de profundidade nos de risco (agente) + zero regressão nova dos fixes + sem bloco pendente. Ou teto (12 iter / 8h) com PARCIAL.
- **Spec fonte da verdade:** `docs/visao/mesa-de-operacao.md` §4-5 (fluxo) + §8 (PII/colisão de canal). Fix cards: FIX-64 (transbordo kanban+handoff), FIX-65 (outbound dossiê), FIX-66 (roteamento inbound), FIX-67 (copiloto+PDF+cassette).
- **Continuidade:** rodada anterior (`2026-06-21-2140-ledger.md`) validou peças ISOLADAS (1-6 integration DB, 68 testes verdes) mas deixou o **E2E browser pendente** e NÃO costurou a coerência ponta-a-ponta. Esta rodada foca a costura.

## Matriz de cenários

| # | Cenário (fluxo) | Origem | Tipo | Status | Bug card | Último resultado |
|---|---|---|---|---|---|---|
| T1 | Transbordo via kanban: POST /api/admin/leads/[id]/transbordo cria `mesa_handoffs` com FKs corretos, status='aberto' | FIX-64 | integration | ✅ | — | handoff.integration + T9 rota verdes (DB real) |
| T2 | Coerência: `administradoraId` do handoff = administradora da cota/proposta Bevi (match por nome/código) → dossiê CERTO | FIX-64 §4.3 | integration | ✅ | — | handoff.integration (CANOPUS→Canopus) + T9 (X não Z) verdes |
| T3 | Outbound: dossiê vai pro WhatsApp do atendente ESCOLHIDO, coerente (cliente/cota/administradora/link), SEM CPF cru | FIX-65 | integration | ✅ | — | outbound.test (det) + T9 rota: sendText p/ atendente, sem CPF |
| T4 | Copiloto orienta com o PDF da administradora DO HANDOFF (não de outra) — builder injeta texto_extraido certo | FIX-67 | cassette+structural+integration | ✅ | — | system-prompt.test + cassette + T9 anti-leak (MANUAL_X não MANUAL_Z) |
| T5 | Inbound: msg do nº do atendente c/ handoff aberto → copiloto responde, persiste `mesa_copilot_messages`, NUNCA vendas | FIX-66 | integration | ✅ | — | routing.integration verde (DB real) |
| T6 | Roteamento sem colisão de canal: nº de atendente de mesa nunca cai em vendas; nº de cliente nunca cai no copiloto | spec §8 | integration | ✅ | — | routing.integration: isMesaAttendantPhone + ack sem-handoff |
| T7 | Guards/idempotência: requireRole admin; transbordo sem proposta; atendente inativo; handoff duplicado | FIX-64 | integration+structural | ✅ | — | handoff.integration (idempotência, lead/attendant not found) + guard tests |
| T8 | E2E browser golden path: admin no kanban → "Transbordar" → escolhe atendente → confirmação/registro | rodada anterior (pendente) | e2e | ✅ | A5 | **spec Playwright NOVO 1/1 verde (3.1s)** + validado ao vivo (MCP): handoff no DB c/ administradora resolvida. **Achou+corrigiu A5.** |
| T9 | **COERÊNCIA E2E (a joia do pedido):** lead+proposta administradora X (com PDF) → transborda p/ atendente Y → handoff aponta X, outbound p/ Y com dossiê da cota, copiloto monta c/ PDF de X e NÃO vaza PDF de Z | spec §4-5 | integration | ✅ | — | **route.integration.test NOVO: 2/2 verde** (rota+outbound+anti-leak copiloto) |

Status: `pendente` · `🟢 verde` · `🔴 vermelho` · `corrigindo` · `revalidando` · `✅ fechado` · `⚠️ bloqueado`.

### Achados (não-cenário) — reportar ao Kairo
- **A1 · DB do develop sem schema da mesa (env drift):** `aja-pg-develop` tinha só 22 migrations/15 tabelas — **nenhuma tabela da mesa**. Mesa mergeada na develop mas ninguém rodou migration no workspace develop → admin/kanban/transbordo davam 500. Dockerfile.dev não auto-migra (dev usa `db:push`/`db:migrate` manual). **Resolvido:** `drizzle-kit push` dentro do container (cria as 5 tabelas mesa + enums + FKs). Migration journal segue com drift (pré-existente). Ver D2.
- **A2 · DELETE de atendente de mesa com handoff referenciando → 500 (gap de produto): ✅ CORRIGIDO (TDD).** `mesa-attendants/[id]/route.ts` fazia `db.delete` cru; FK `ON DELETE no action` → 23503 (500). **Fix:** guarda antes do delete → 409 gracioso ("desative em vez de remover; histórico preservado") quando há handoff; 200 sem handoff. Teste `route.delete-guard.integration.test.ts` (falhou→verde). Decisão de design reversível (§4.3.1): bloquear hard-delete preservando auditoria, desativar é o caminho.
- **A5 · 🔴→✅ BUG CRÍTICO: transbordo via kanban estava 100% QUEBRADO na UI (CORRIGIDO, TDD).** O dialog `mesa-transbordo-dialog.tsx` lia `data.attendants` da resposta da API, mas o endpoint retorna `{ mesaAttendants: [...] }` (a tabela já lia certo) → lista SEMPRE vazia → "Nenhum atendente de mesa ativo cadastrado" mesmo com atendentes ativos → **o admin nunca conseguia transbordar pelo kanban**. Achado dirigindo o browser real (MCP). Os integration tests não pegaram: cobrem API e dialog isolados, nunca o contrato de shape entre bloco-a e bloco-b. **Fix:** dialog lê `data.mesaAttendants ?? data.attendants ?? []` (tolerante). Regressão `mesa-transbordo-dialog.test.tsx` (happy-dom, falhou→verde) + E2E spec re-rodável. Validado live: handoff criado pelo kanban.
- **A4 · Typecheck do repo com erros PRÉ-EXISTENTES (fora do escopo):** `tsc --noEmit` acusa 5 erros em arquivos não tocados por esta rodada (`formatter.moto.test.ts` buttons, `web-resume/same-device.spec.ts` reducedMotion, `agent-flow.eval.test.ts` return, `migrate-guard.test.ts` ts-expect-error). Não é regressão do transbordo; o pre-commit gate é vitest (verde). Reportar pra bloco próprio.
- **A3 · Dívida de isolamento de teste (§4.7 bloco #1):** suíte integration da mesa usa whatsapp hardcoded compartilhado (`5562988887777`) entre arquivos → colisão FK no DB compartilhado quando rodam juntos (mesa-attendants falha). Já no inbox (`2026-06-21-divida-infra-teste-qa-noturno.md`). Fix barato: phones únicos por teste.

## Linha do tempo
- 00:00 — ledger criado. App reiniciado limpo na qa-base, warmup em background. Explore mapeando o fluxo. Rodando baseline determinístico.
- 00:55 — baseline determinístico host: 270/271 (1 fail = cassette híbrido que toca DB no host = `.env.local` aponta host morto).
- 03:55 — **causa-raiz: DB develop sem schema mesa.** `db:push` no container criou as tabelas. handoff.integration + routing.integration VERDES no container (T1,T2,T5,T6). mesa-attendants falha junto (colisão A3) mas passa isolado.
- 04:02 — **T9 NOVO (route.integration.test) 2/2 VERDE**: coerência ponta-a-ponta provada (rota+outbound+anti-leak copiloto). T1–T7,T9 ✅. Falta T8 (E2E browser) + tratar A2 (DELETE 500) com TDD.
- 04:09 — A2 corrigido (TDD, 409 gracioso). 04:18 — **A5 BUG CRÍTICO** achado no browser (transbordo via kanban quebrado — chave errada da API), TDD fix. 04:20 — T8 validado AO VIVO (handoff via kanban) + spec Playwright re-rodável 1/1 verde. 04:24 — A3 fix (phone único): suíte mesa inteira 10 arq/51 testes VERDE junta.
- 04:29 — **REGRESSÃO FINAL** suíte completa container: 2034 passed, 1 failed (**A6**, fora do escopo). pre-commit host 1869 verde em todos os commits.

## Status final — todos os cenários do escopo ✅
- **T1–T9 ✅** (integration DB real + E2E browser real + spec re-rodável). **2 bugs de produto corrigidos (A2, A5) + 1 atrito de ambiente consertado na fonte (D2/D4) + 1 fix de isolamento (A3).** 4 commits na qa-base.
- **Gate de profundidade (§4.2.1):** N/A pro escopo — a mesa não computa número fiscal por LLM (dossiê vem da proposta, formatado deterministicamente); o copiloto (agente) é coberto por cassette (Camada 2) + integration mocka o LLM deterministicamente; eval real é nightly (Camada 3). Sem mutante/pass^k a rodar aqui.
- **A6 · Falha PRÉ-EXISTENTE fora do escopo (reportar):** `src/lib/memory/letta-adapter.integration.test.ts` (busca semântica) falha — Letta shared `Up (healthy)` mas `archival-memory timed out after 8000ms` → 0 hits. NÃO toca o transbordo; meus commits não tocam `src/lib/memory`. Ambiental (Letta archival lento). Também: `recordMemoryEvent` falha ao inserir em `memory_events` (drift de schema do DB dev pós-push, best-effort, não derruba app).
- **Promoção develop = PENDENTE-KAIRO** (blast radius — §9). qa-base `qa-base/2026-06-22-transbordo-mesa-whatsapp` pronta pra revisão.
