# LEDGER — QA Autônomo · Frente 3: Mesa de operação (transbordo + broadcast + claim + copiloto + kanban)

- **Rodada:** 2026-07-01 02:36
- **Branch de trabalho:** `qa/mesa-operacao` (worktree Superset, ancorada na develop `4c8a81c5`)
- **Onda validada:** `divergencias-jornada` (FIX-123..126 na minha área)
- **Fonte da verdade:** `docs/jornada/jornada-canonica.md` (Parte 2 — EDIÇÃO #6, transbordo auto-broadcast + claim, linhas 141-147; Mapa D14-D17 linhas 239-242)
- **Faixa FIX:** FIX-170 a FIX-189 (só numero nessa faixa)
- **Stack:** `aja-app-mesa-operacao` :3010 (`http://aja-mesa-operacao.orb.local`) + `aja-pg-mesa-operacao` :5434 — migrations aplicadas no container (enum `em_atendimento` OK, `mesa_attendant_id` nullable OK)

## Critério de PRONTO (binário)
Todos os cenários 🟢 vivo ∈ {✅ fechado} + zero regressão nova + gate de profundidade nos cenários de corrida/claim, OU teto (12 iter / 8h).

## Cenários

| # | Cenário (fluxo) | Origem | Fase | Nível certo | Status | Fix | Último resultado |
|---|-----------------|--------|------|-------------|--------|-----|------------------|
| 1 | (a) Lead entra em `na_administradora` → transbordo dispara SOZINHO (sem clique): cria handoff sem dono + broadcast | D14/FIX-123 | 🟢 vivo | integration (worker→DB) + structural | ✅ fechado | — | verde (integration `proposal-status-poll.integration.test.ts` + structural) 02:44 |
| 2 | (b) Dossiê vai a TODOS os atendentes ativos, cada um com botão "Vou atender" (não single-cast texto) | D15/FIX-124 | 🟢 vivo | structural + integration | ✅ fechado | — | verde (`outbound.broadcast.test.ts` + `claim-dispatch.integration.test.ts` + cassette FIX-124) 02:44 |
| 3 | (c) **CORRIDA**: 2 atendentes clicam "Vou atender" → exatamente 1 assume, outro recebe "já foi assumido" | D16/FIX-125 | 🟢 vivo | **integration de corrida (OBRIGATÓRIO)** + stress | ✅ fechado | — | verde (`handoff.claim.integration.test.ts`) + **DEPTH GATE: stress 8 atendentes × 25 rodadas = 200 claims concorrentes, sempre 1 vencedor** 02:41 |
| 4 | (d) Ao assumir (claim), a raia muda `na_administradora → em_atendimento` (forward-only, lead_events) | D17/FIX-126 | 🟢 vivo | integration (DB) + structural | ✅ fechado | — | verde (`handoff.claim.integration.test.ts` FIX-126 + `handoff.raia.test.ts`) 02:44 |
| 5 | (e) Copiloto responde só ao DONO do handoff — não vaza pra quem não assumiu | jornada §150-153 + FIX-124 | 🟢 vivo | cassette (Cam.2) + integration | ✅ fechado | — | verde (`claim-dispatch.integration.test.ts` "não-dono NÃO vaza" + cassette FIX-124 + `routing.ts` só resolve handoff do próprio dono) 02:44 |
| 6 | (f) Dados sensíveis (CPF/documentos) NÃO trafegam no WhatsApp — dossiê é whitelist sem PII | spec §8 LGPD + FIX-124 | 🟢 vivo | structural/unit | ✅ fechado | — | verde (`outbound.test.ts` regex anti-CPF; DTO sem campo cpf por construção) 02:44 |
| 7 | Idempotência: re-poll do mesmo lead em `na_administradora` NÃO cria 2º handoff; não-gatilho não transborda | D14/FIX-123 | 🟢 vivo | integration | ✅ fechado | — | verde (`proposal-status-poll.integration.test.ts` FIX-123 dedup + não-gatilho) 02:44 |
| 8 | Isolamento de falha: broadcast/outbound falhando NÃO derruba a transição de raia nem o ciclo do worker | D14/FIX-123 | 🟢 vivo | integration behavioral | ✅ fechado | **FIX-170** | ERA verde-frágil (só structural grep de try/catch). Add teste behavioral + **mutation-verified** (removi try/catch → vermelho; revertido → verde) 03:00 |
| 9 | Precedência de canal: clique/texto de atendente de mesa vai pra mesa (claim/copiloto), NUNCA pro funil de cliente | FIX-124 + spec §8 | 🟢 vivo | structural + integration | ✅ fechado | — | verde (cassette FIX-124 + processor precedência texto+interativo) 02:44 |
| 10 | **GOLDEN PATH E2E** (kanban): admin transborda lead → dialog broadcast (sem single-select) → handoff SEM dono no DB, administradora resolvida | D14-D16 + FIX-124 | 🟢 vivo | **E2E browser (assertion de VALOR no DB)** | ✅ fechado | **FIX-171** | ERA STALE (testava combobox removido pelo FIX-124 → vermelho). Reescrita pro fluxo broadcast + **rodada VERDE no container** (chromium Alpine + orb.local) 05:53 |

## Achados / correções (faixa FIX-170..189)
- **FIX-170** (test hardening, INLINE): cenário 8 (isolamento de falha) só tinha cobertura STRUCTURAL (grep de `try{}catch` no source) — o card FIX-123 §Regressão exigia o caso behavioral e ele não existia (verde frágil). Add `src/lib/workers/proposal-status-poll.fail-isolation.integration.test.ts` (2 casos: broadcast quebra → handoff+raia sobrevivem; dispatch quebra → raia persiste + ciclo não derruba). Mutation-verified (remoção do try/catch → teste vermelho). Produto estava correto; só a rede de regressão tinha buraco.
- **FIX-171** (stale E2E, INLINE): `tests/e2e/specs/admin-mesa-transbordo/golden-path.spec.ts` testava o SINGLE-SELECT de atendente que o FIX-124 REMOVEU (combobox "Atendente de mesa" + handoff COM dono). Ia vermelha (confirmado: timeout no combobox). Reescrita pro fluxo broadcast (sem select, handoff SEM dono). Infra E2E no container Alpine habilitada em `playwright.config.ts` (gated por `PW_EXECUTABLE_PATH` + video off no container — inerte no host/CI). Rodada verde.

## Observações pro Kairo (decisão de produto — não bug de código)
- **OBS-1 (handoff órfão sem atendente ativo):** se 0 atendentes de mesa ativos, o transbordo (auto OU manual) cria o handoff mas ninguém é notificado. No manual, a rota devolve `outboundError: "nenhum atendente de mesa ativo"`, mas o dialog o engole (fecha como sucesso). No auto (worker) é silencioso. Cliente que contratou fica sem atendimento e sem sinal. É decisão de produto: (a) surfacar aviso no kanban quando broadcast vai a 0 atendentes; (b) alerta operacional. Não "consertei" — é UX/produto (§4.3.1).

## Log de transições
- 02:36 — Ledger criado. Stack up, migrations aplicadas no container. Anchoring concluído (4 cards + decisão + jornada + código real lido).
- 02:44 — Suíte da mesa no container: 98 structural+integration + 16 integration verbose (corrida, auto-transbordo, raia, não-vaza, idempotência, não-gatilho) — TODOS verdes. Cenários 1-7,9 ✅.
- 02:41 — Depth gate cenário 3 (corrida): stress 8×25 = 200 claims concorrentes → sempre 1 vencedor. ✅
- 03:00 — FIX-170: teste behavioral de isolamento de falha (cenário 8) + mutation-verified. 100 testes mesa verdes.
- 03:05 — Gate `pnpm test:unit` no container: **216 arquivos, 2194 testes, 0 falhas**.
- 05:53 — FIX-171: E2E golden-path do kanban reescrita (stale→broadcast) + infra container, rodada VERDE. Cenário 10 ✅. Sibling `admin-mesa-cadastros` também verde (não-stale).
- **STATUS: todos os 10 cenários ✅. Pronto pra commit + finalização.**
